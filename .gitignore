toolchain/
.deps-cache/
third_party/slang-prebuilt/
build/
build-*/
.cache/
.claude/
rendererx-samples-*.zip
ctest-results-*.xml

# Phase 4 Stage 1 Task 13: tools/fetch_assets.sh downloads Khronos
# glTF-Sample-Assets test content (DamagedHelmet mandatory, Sponza
# --sponza-only) here -- never committed (D16), fetched/checksum-verified
# on demand for local/CI testing only.
assets/fetched/

# Phase 5 Stage 1 Task 11 (#47): tools/gltf_conformance's own pinned
# Playwright install (npm ci output) -- reproducible from package.json/
# package-lock.json, never committed (same posture as every other
# language-ecosystem dependency tree in this repo).
tools/gltf_conformance/node_modules/
