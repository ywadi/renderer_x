# SDD ledger — plan: docs/superpowers/plans/2026-08-10-phase3-render-graph-materials.md

Spec: docs/superpowers/specs/2026-08-10-phase3-render-graph-materials-design.md
Research: research-rendergraph.md, research-abi-materials.md (this directory)
Spec+plan authored by coordinator directly (per operating mandate), committed a482c9f.
Pre-flight plan scan: no task/constraint conflicts found.

Task 1: implementer DONE_WITH_CONCERNS, commit 16ccad4 (base a482c9f). 11/11 doctest cases, both presets build. Attribution verified clean by coordinator.
Task 1 ruling (coordinator): pass-kind inference from attachments (has color/depth output = raster stages for storage access; none = compute stages) is ACCEPTED as the intended long-term rule — raster passes require attachments under dynamic rendering, so the inference eliminates an invalid state rather than guessing; QueueClass stays a pure scheduling hint. Task 2/3 briefs carry this resolution.
Task 1 review: spec PASS, Approved w/ 1 Important (no cycle detection in compile — silent empty graph), 2 Minor. Fix round 1 dispatched to original implementer for the Important + coordinator-resolved mapping extension (texture-input stage follows pass-kind inference: FRAGMENT_SHADER raster / COMPUTE_SHADER compute) + doc-comment tighten. Minor (doc comment) folded into round; spec-gap minor resolved by coordinator ruling above.
Task 1: complete. Commits 16ccad4 + fix 089af68 (both attribution-verified). Re-review: all findings addressed; probes confirmed multi-root cycle detection and harmless discard of dead cyclic subgraphs.
Task 1: minor (deferred): dead cyclic subgraphs are silently culled rather than diagnosed — sensible, lightly documented; polish candidate for final review triage.
Task 2: implementer DONE_WITH_CONCERNS, commit df40111 (base 089af68). 20/20 cases, 149 assertions, both presets. Attribution verified clean.
Task 2 concern 1 (coordinator preliminary ruling, reviewer to scrutinize): Task 1 binds readers to a resource's FINAL declared writer -> cross-pass WAR inexpressible via public API; implementer unit-tested WAR against ResourceBarrierState/applyAccess directly (new public names beyond brief). Preliminary ruling: accept reads-observe-final-version as Phase 3 semantics (multi-version chains use distinct names, matching Granite's rename-on-RMW convention) IF documented in public header; reviewer to verify documentation + assess the added API surface.
Task 2 concern 2: six tests relaxed from "only barrier in list" to "exact 6-field barrier present by physicalIndex" (reading passes needed a backbuffer write to be Graphics-class). Reviewer to verify no vacuous passes.
Task 2 review: spec FAIL (Granite fidelity), 1 Critical (aggregate invalidate tracking drops barriers for second reader in new stage — probe-verified under-sync), 1 Important (unratified public surface), 2 Minor. Fix round 1 dispatched to original implementer. Coordinator rulings: ResourceBarrierState/applyAccess move to rx::graph::detail (ratified as test/executor seam, not API-stable); relaxed tests strengthen to exactly-one-per-physicalIndex; report metric corrected.
Task 2: complete. Commits df40111 + fix 77f42cf (attribution-verified). Re-review: all findings addressed; Critical confirmed dead via independent probe re-run; WAR reader-stage-union divergence from Granite confirmed correct per Vulkan sync model and documented in code; extended sync2 stage bits (>31) probed OK.
Task 3: implementer DONE_WITH_CONCERNS, commit 73f8c4a (base 77f42cf). 22/22 device-free + 3/3 GPU cases, zero validation errors, both presets. Attribution verified clean. Concerns: (a) additive API extensions passAt()/Device::instance() — reviewer to assess; (b) ci.yml windows exclusion regex misses rx_graph_gpu_tests — CARRIED to Task 4 dispatch (must fix before push); (c) implementer found+fixed pooled-buffer first-use sync gap with new GPU test — reviewer to verify the fix's correctness.
Task 3 review: spec FAIL, 1 Critical (finalStageThisExecute overwrites instead of unions across passes — probe-verified under-sync next frame), 2 Important (ci.yml windows exclusion missing — coordinator-caused contradiction, ruling SUPERSEDED: fix in this round, not Task 4; invokeExecute/PassContext unguarded UB path), 2 Minor (eviction off-by-one vs ruling; imageFormat silent UNDEFINED on buffers). Fix round 1 dispatched to original implementer. Ruling on Critical: union ALL touching stages across the execute (conservative over-sync accepted).
Action item (from Task 3 review observation): synchronization validation (VK_VALIDATION_FEATURE_ENABLE_SYNCHRONIZATION_VALIDATION_EXT) is not enabled anywhere, so --validate cannot catch under-sync bugs like the Task 2/3 Criticals. Plan: fold into Task 4 dispatch — enable sync validation in the GPU test gates/context validation path so graph execution is sync-validated continuously.
Task 3: complete. Commits 73f8c4a + 980031f + 5c4aa23 (attribution-verified). Re-review: 6/6 findings addressed, no new defects; misuse path closed at compile level; stage-union probe-verified order-independent; CI exclusion verified against real ctest registry.
Tasks 4+5: dispatched IN PARALLEL (worktree isolation, both Sonnet, base 5c4aa23). Task 4 carries the sync-validation action item (VkValidationFeaturesEXT SYNCHRONIZATION_VALIDATION in context.cpp validation path). File disjointness verified: T4 = samples/05+shaders/multipass+packaging+ci+rx_rhi_vk/context.cpp; T5 = rx_material+rx_graph pass_signature/pass.h/executor.cpp+shaders/material+src/CMakeLists.txt.
Task 5: implementer DONE, commits 51f731c (+report bb30409) on branch worktree-agent-a12b9aa263ca60061 (ff'd to base 5c4aa23). 6 cases/64 assertions + full suite 12/12, zero validation errors, both presets. Attribution verified clean. Note: T5 edited ci.yml exclusion regex (required by its own gate) — MERGE OVERLAP with Task 4's ci.yml edits; resolve at sequential merge. RX_MATERIAL_SHADER_DIR baked path flagged — carry to Task 8 (ship-relative lookup).
Task 5 review: spec PASS (15/15, probe-verified incl. Slang reflection + specialization mechanism + corrupt-cache + name-collision), Approved w/ 2 Minor.
Task 5: minor (deferred): no automated test for corrupt-pipeline-cache-content path (absent/valid covered; behavior probe-confirmed).
Task 5: minor (deferred): MaterialSystem::layoutInfo() returns reference invalidated by HandlePool reallocation on later loadMaterial — hazard documented on Impl::materialHandles but not on the public accessor.
Task 5: complete. Merged to main via cherry-pick: 5237915 (code) + 1b83f6c (report). Post-merge rx_graph+rx_material tests 3/3 green on main.
Task 4: implementer DONE, commit 27931d3 on branch worktree-agent-a0257a83d60b1accb (base 5c4aa23; report inside commit). 12/12 linux ctest incl. sample gate w/ sync validation active; windows build clean. Attribution verified. Review dispatched — scrutiny: third false-positive guard (A/B-tested stale-layer claim), shader struct-drift fix, analytic pixel gate, ci.yml overlap with T5 to resolve at merge.
Task 4 review: spec PASS (8/8, independently reproduced incl. false-positive guard ablation against both layer versions, pixel math re-derivation, standalone packaged run), Approved w/ 1 Low-Medium (ObjectTransform triplicated, comment-guarded only). Fix round 1 dispatched: single-source scene_types.slang + sizeof static_assert + index-1 probe. Ledgered: rx_shader::reflect() lacks storage-buffer stride exposure — enhancement candidate (blocks reflected stride asserts), triage at final review or Phase 4.
Task 4: complete. Commits merged to main as d519ae6 + 04fca66 + 2ead2d0 (attribution-verified at each round). Fix rounds: 1 (scene_types.slang single-source + drift guards), 2 (channel-order-exact probe). Re-review: all findings addressed.
Post-merge verification on main: full linux-native suite 13/13 green (incl. sample_05 gate w/ sync validation), windows-cross-zig build clean. Worktrees removed, branches deleted.
Tasks 1-5 COMPLETE. Dispatching Task 6 (COM-lite public surface).
Task 6: implementer DONE, commit 6c49910 (base 2ead2d0). 9 device-free + 9 GPU cases, full suite 14/14, both presets, warnings-clean audit self-run. Attribution verified. Flagged for Task 7: wire reloadChanged, collapse duplicate reflection session into MaterialSystem, connect param blob to GPU (field offsets), real IRxTexture creation path. Review dispatched.
Task 6 review: spec FAIL (exceptions can cross ABI in 3 setters), 1 High + 2 Medium + 1 Low. Verified clean under hardest scrutiny: refcount acq_rel ordering (independent TSAN stress 0 races), COM identity, GUID memcmp soundness, header self-containment, null-internal audit. Fix round 1 dispatched: boundary catches + loadMaterial ordering (orphan prevention) + session-config parity + TDD evidence honesty; added structural never-throw test + orphan-count regression.
Task 6: complete. Commits 6c49910 + fix 0b0baab (attribution-verified). Re-review: all 4 findings closed, no new defects; bonus: latent same-pointer setTexture use-after-free eliminated. TSAN-verified refcounting; header self-containment independently recompiled.
Task 7 dispatch carries Task 6 handoffs: wire reloadChanged, collapse duplicate reflection session (field-level reflection into MaterialSystem), param blob -> GPU binding via field offsets, real IRxTexture creation path (pre-release GUID regen on IRxMaterialSystem documented as intentional).
Task 7: implementer DONE, commit 067fdde (base 0b0baab). 22 GPU cases/296 assertions + arena tests, full suite 14/14, both presets, zero validation errors. Attribution verified. Review dispatched — scrutiny: setTexture silent index-0 fallback for foreign IRxTexture (coordinator skeptical: silent-wrong-bind vs error code; QI-based engine-type detection is the COM-correct RTTI-free check), reload teardown immediacy vs Vulkan lifetime rules, reflection collapse completeness (duplicate session truly gone).
Task 7 review: spec FAIL via F1 (setTexture silent index-0 fallback + RX_OK for foreign IRxTexture — coordinator's flagged suspicion confirmed as High), F2 Medium (no standalone ParamArena frames-in-flight/exhaustion test). Verified clean under scrutiny: reload-teardown legality checked against real Vulkan-Docs VUID text (implementer vindicated), reflection collapse confirmed by tree grep, hot-reload invalidation asserts pipeline identity, DescriptorArena exhaustion/reset sound. Fix round 1 dispatched: private-GUID QI check -> RX_E_INVALIDARG for foreign textures (FakeTexture tests adapted, not deleted) + standalone ParamArena isolation/exhaustion tests.
Task 7: complete. Commits 067fdde + fix 648021e (attribution-verified). Re-review: all findings closed (private-GUID rejection with re-homed refcount coverage; standalone ParamArena isolation/exhaustion tests with real Vulkan failure lines observed); cursor-waste note rated acceptable-as-documented (cannot cause premature exhaustion within the 512/frame ceiling). Library work for Phase 3 is DONE. Dispatching Task 8 (sample 06).
Task 8: implementer DONE, commits 62611b5 (+report df7125e), base 648021e. 15/15 linux ctest incl. new gate; packaged standalone verified native+Wine; 3 hot-reload cycles clean. Attribution verified. CENTRAL REVIEW QUESTION: implementer discloses material-shader-side texture sampling (evaluate() reading a bindless texture) is unwired in rx_material core — dispatch required createTexture2D exercised end-to-end in a material; reviewer to characterize what was actually delivered vs the requirement. Two self-found bugs fixed (dangling bindless pointer via Scene-by-value; mesh winding vs fixed backface culling).
Task 8: complete. Commits 62611b5 + df7125e (attribution-verified). Review: spec PASS, Approved w/ 2 Low. Rulings: F1 (texture sampling inside material evaluate() unwired) = defensible per reviewer; LEDGERED as a bounded dedicated task ("wire bindless texture sampling into material.slang/forward_entry + reflectMaterialLayout allow-list + GPU test") committed as the FIRST task of Phase 4's material work — recorded in phase 4 seed notes. F2 (getPipeline doc claims no-culling; real state is backface-CCW) = folded into Task 9 as a one-line doc fix.
Task 9 dispatched (Haiku): docs, DepCache CMAKE_ARGS hashing, README/roadmap, abi.md, layer-table tick, F2 doc fix.
Task 9: implementer DONE, commit 47135af (attribution-verified). Review: spec PASS, Approved w/ 1 Medium (CMAKE_ARGS hash lacks delimiter escaping — ["a;b"] vs ["a","b"] collide, proven via cmake -P harness; dormant at current call sites) + 2 Low (report line-count typo; windows-ctest note informational). Fix round 1 dispatched to Haiku implementer: length-prefix escaping + report correction. Low #2 needs no action.
Task 9 re-review round 2: Medium REOPENED (semicolon case still collides through real call path) + new Low (stale report section). COORDINATOR ADJUDICATION on the Medium: NOT A DEFECT in the hash — CMake list semantics make ["a;b"] and ["a","b"] the same value upstream of the key AND in the dependency build itself (lists flatten to identical command lines), so identical keys are semantically correct; the pipe/encoding collision (our internal join char) was the real defect and IS fixed by length-prefixing. Residual defect = header comment overclaims semicolon protection. Fix round 2: comment honesty + stale report section. Reviewer to confirm the adjudication's factual basis.
Task 9: complete. Commits 47135af + b4728b5 + cc95b34 (attribution-verified each round). Re-review round 3: all findings addressed, adjudication CONFIRMED empirically (identical argv both spellings; no same-key/different-build path). Full gate at HEAD: 15/15, both presets, 4/4 dep-cache hits.
ALL 9 TASKS COMPLETE. Dispatching final whole-branch review (range 2d48444..HEAD).
Deferred minors for final-review triage: [T1] dead cyclic subgraphs silently culled, lightly documented; [T5] no corrupt-pipeline-cache-content regression test; [T5] MaterialSystem::layoutInfo() reference invalidated by HandlePool reallocation — undocumented on public accessor; [T4-carried] rx_shader::reflect() lacks storage-buffer stride exposure (drift-guard enhancement); [T9-informational] windows-cross has no ctest runner for GPU/present paths (by design, manual rows exist).
FINAL WHOLE-PHASE REVIEW: RELEASE-READY. Findings: [Medium, dormant, CARRY->Phase4 card #19 criterion] PassSignature 8-slot clamp vs uncapped rendering-info mismatch if >8 color outputs (none today; add loud compile() bounds-check with history-resources task); [Medium-High, pre-tag] commit untracked SDD audit trail (this action); [Low, pre-tag] abi.md "DLL" wording fixed by coordinator; README sequencing informational. All 5 deferred minors ruled CARRY with rationale (see final-review.md). Attribution sweep clean at content level across whole range. 15/15 both stale-cache and forced-rebuild.
CI RUN 1 (acfce89): linux FAILED — 2 suites (rx_rhi_vk_tests DescriptorArena exhaustion, rx_material_gpu_tests ParamArena pool-exhaustion sub-case). ROOT CAUSE: tests asserted driver-optional VkDescriptorPool strict enforcement; lavapipe over-allocates (spec-legal). Real contract bug: arenas must self-enforce documented budgets. SECONDARY FINDING: sync validation silently inactive on CI — VK_EXT_validation_features probe not layer-scoped. Build times doubled as predicted (one-time dep-cache key invalidation). Fix wave dispatched (Sonnet).
Release fix wave: f4ce432 (attribution-verified), re-review CLEAR TO PUSH. Fixes: DescriptorArena/ParamArena self-enforced budgets (driver-optional pool enforcement no longer assumed); ci.yml installs vulkan-validationlayers. LEDGERED HONESTLY: all Vulkan validation (core, not just sync) was INACTIVE on CI through run acfce89 — every prior CI "0 validation errors" pass was vacuous; the bar was only enforced locally until now. Known risk for next run: the sync-validation separate-sampler guard matches literal message substrings verified against layers 1.3.204.1/1.4.357 only; unpinned apt layer on ubuntu-latest may emit unmatched variants -> watch for unguarded SYNC-HAZARD lines.
