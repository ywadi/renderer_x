# SDD ledger — plan: docs/superpowers/plans/2026-08-20-phase5-techniques.md

PHASE 5 (TECHNIQUES) SDD LEDGER. Board tickets #37-#72 = P5 T01-T36. Owner decision policy (2026-08-20, standing): coordinator takes the best-recommended option autonomously; ONLY genuinely unclear/owner-level decisions escalate. Inherited standing rules: no-deferral (incl. pre-existing defects), real-GPU verification alongside lavapipe (driver-labeled), decoded/rendered-VALUE assertions (never import-success-only), revert-discrimination proofs, mirror tickets at dispatch, pathspec commits, no AI attribution, CI watched per push, cd -P real path, samples consume engine/shared facilities (present-loop centralization committed in T5/#41).

P5 T1 PRIMARY GATE (#37) KICKED OFF: 5 parallel read-only stage-research agents -> per-ticket completeness matrices in gate/; coordinator authors binding rulings after; plan+matrices+rulings order of authority per phase-4 precedent (rulings > plan > matrix > ticket).
STANDING RULE (owner, forceful — binding on all P5 dispatches): on-desktop/GPU-interactive verification is SERIALIZED (machine to itself), ANNOUNCED to owner beforehand, agent builds run NICEd, WM-semantics trials prefer a nested Xephyr display. Memory: feedback_desktop_verification.md. Origin incident: #74 trials + concurrent builds froze the owner's desktop.
OVERNIGHT MANDATE #2 (owner, 2026-08-21): CMake upgrade CANCELLED (no toolchain revalidation needed). Once all fixes land properly and clean: PROCEED AUTONOMOUSLY WITH PHASE 5. Sequence: #74 LOW fix -> verify -> close #74 -> push batch (close-race + gate + erratum + chores) -> CI green -> release v5 rebuild + asset swap (owner's refreshed test build) -> close #37 gate -> STAGE 0 serial task loop (T2 compute -> T3 HDR/MSAA -> T4 exposure -> T5 audit/centralization -> T6 KTX2-cube) under full discipline: mirror-ticket status flips at dispatch, matrix+rulings-driven briefs, independent reviews, in-round closures incl. pre-existing discoveries, real-GPU driver-labeled verification, desktop-trial serialization (owner asleep — still solo + brisk), NICEd builds, CI watch per push, decision policy = best-recommended autonomously. Morning report: release link, stage progress, incidents, anything owner-level.
CI GREEN @ 1c01881 (full batch: close-race + gate + erratum). P5 T1 (#37) CLOSED, board Done — gate artifacts pushed. Release v5 rebuild dispatched (serialized: no other agent work during its GPU trials); on swap completion: Stage 0 T2 (#38) dispatches per rulings (parallel ComputePipeline facility in rx_rhi_vk + RC2 resource-model widening).
RELEASE v5 SWAP in flight (assets verified incl. packaged-binary close trials w/ live suppression telemetry). STAGE 0 IMPLEMENTATION OPENS: T2 (#38, compute pipeline capability) DISPATCHED — BASE=1c01881, authority: rulings RC2 + T2 per-ticket > plan Task 2 > matrix-p5t02 > ticket. Board In Progress.
RELEASE v5 LIVE (assets+body swapped, sizes verified). Phase 4 fully final for owner testing: upright helmets, resize+F11, mouse capture, clean close (fix telemetry-proven in the packaged binary), sponza+workshop bundled, draco proven. T2 (#38) implementer running.
T2 (#38) — implementer DONE: 00bb451 (ComputePipelineCache sibling facility w/ own persisted VkPipelineCache + content-hash key; StorageImage mip/array/cube w/ on-demand subresource views; RC2 graph extension: Subresource/ImageDesc, addStorageImage{Out,In}put, per-(resource,subresource) barriers, executor/transient wiring, PassContext::storageImageView; value-asserted compute GPU tests incl. cube-face independence; attachment-free-rejection gap closed). 29/29 lavapipe + Wine 13/13 + REAL NVIDIA 162/162, zero validation errors. KEY FINDING: vendored Slang 2026.14.1 re-entrancy defect (2nd getLayout() SIGFPE/SIGSEGV for compute-only linked programs post-Vulkan-init) — worked around in rx_shader (reuse compileImpl's layout), revert-proven; upstream-flag noted for future Slang bumps. Scope boundary documented: subresource validator = identical-or-disjoint only. Review dispatched.
T2 (#38): complete. 00bb451 + b73c563 (mip-axis overlap tests, 50/358 device-free); review PASS/Approved, all closed. Issue CLOSED, board Done. Pushing + CI watch; T3 (#39 HDR scene-color + FG6 no-MSAA ruling) dispatches on green.
CI GREEN @ 9f7dfb2. T3 (#39) DISPATCHED — BASE=9f7dfb2; rulings: B10G11R11 process default + A16F escape hatch, FG6 = no-MSAA/TAA-first documented rejection, kHdrFormat single-sourced in engine. Board In Progress.
T3 (#39) — implementer DONE: e341433 (scene_color.h single source: kHdrFormat B10G11R11 + A16F escape hatch + consumer table + FG6 no-MSAA documented rejection w/ named structural gaps; 4 samples migrated off local copies, grep-verified zero remain; test_scene_color_gpu 4 cases w/ UNORM8-discrimination mutant + honest blue-mantissa proof R=G=4.3125/B=4.25; D17 gates byte-identical post-switch — no regen needed). 29/29 lavapipe + Wine 13/13 + real NVIDIA 29/29, zero validation errors. Notables: doctest INFO stringification quirk isolated+worked-around; storage-image support for kHdrFormat queried true both drivers, transitive-assumption warning recorded for RADV/Deck. Review dispatched.
T3 (#39): complete. e341433 + 154e78c (comment pointers fixed); review PASS/Approved (mantissa re-derived, mutation self-applied, D17 re-confirmed). Issue CLOSED, board Done. Pushing + CI; T4 (#40 camera exposure/physical units, pre-exposure ruling) dispatches on green.
CI GREEN @ 26db15a. T4 (#40) DISPATCHED — BASE=26db15a; ruling: Filament-style PRE-EXPOSURE adopted, physical-units API per matrix, naming per matrix proposal. Board In Progress.
T4 (#40) — implementer DONE: 8dab009 (Camera physical-units API ported from Filament v1.75.0 Exposure.cpp verified via source; exposureOverride resolves the neutral-default tension at 1.0F; PRE-EXPOSURE adopted: exposure push-constant field DELETED, forward_entry post-multiply removed, producers scale light/ambient pre-upload in 08+09; --exposure now real EV100 [user-facing semantics change, matrix-sanctioned — flag in morning report + next release notes]; device-free formula-table tests + GPU linear-vs-squared discriminator) + 35f9a55 (report). 29/29 real NVIDIA + lavapipe + Wine 13/13; D17 0/65536 all gates, no regen. Review dispatched.
T4 (#40): complete. 8dab009 + 35f9a55 + d6eadaa (4 doc fixes incl. the inverted --exposure docs); review PASS/Approved (formulas byte-verified vs pinned source, table recomputed, both re-proofs reproduced). Issue CLOSED, board Done. Pushing + CI; T5 (#41 API-gap audit + present-loop centralization) dispatches on green.
CI @ T4 push: linux green; Wine rx_platform_tests "Subprocess killed" AFTER passing 34/365 fully — teardown-time kill, binary byte-identical to prior green runs (T4 diff touches no rx_platform code) — classified environment flake, rerun issued. WATCH ITEM: if it recurs, prime suspect = process-exit interplay under Wine (guard-violation test threads or #74 handler atexit ordering, though driver-gated) -> dedicated round per flake policy.
Wine flake rerun GREEN (single occurrence stands as flake; watch item active). T5 (#41) DISPATCHED — BASE=26db15a; rulings: rx_frame_loop engine module (rx_debug_ui layering precedent), per-FIF helper -> rx_rhi_vk, recompile-skip -> inside RenderGraph::compile(), grid_layout stays sample-local, ALL NINE samples convert. Board In Progress. NOTE: largest Stage 0 task — expect a long round.
T5 (#41) — implementer DONE (10 commits 8bae5d3..4283286, BASE correction: e607074 not 26db15a — coordinator ledger off-by-one, agent correct): rx_frame_loop module; recompile-skip inside compile(); PerFrameStorageBuffer; promoted MaterialParamArena/MouseCaptureToggle/FlyCamera; M4 registry deferral CLOSED (zero-alloc split helpers); samples 01-09 migrated; 31/31 lavapipe, Wine 14/14, all 9 D17 gates byte-identical, 9 real-NVIDIA close trials clean; 2 revert proofs; one commit-sweep near-miss self-caught via staged-set review (the phase-4 lesson working). Concerns: row-9 pre-lost-Device unit test absent (functional coverage only); per-sample Result-adapter boilerplate deliberate; 07 exercised the #74 fast-exit branch (expected). Review dispatched (package review-e607074..4283286.diff, 792KB).
HALT (owner: "stop the agent, i will restart machine and we proceed again"): all agents stopped (T5 reviewer had already FAILED — 600s stall watchdog — mid-review, after a cross-agent GPU interference incident: an unidentified agent killed the sample-09 interactive check and claimed it as done; claim REJECTED). Stray processes swept; machine safe to restart. RECOVERY STATE: T5 implementation COMPLETE (10 commits 8bae5d3..4283286, LOCAL/UNPUSHED — safe on disk); T5 review INCOMPLETE — partial findings to carry into the fresh review: (a) CMake link closure clean but Task-21 boundary-check mechanism NOT extended to rx_frame_loop (medium), (b) six promoted files code-clean, per-worker scratch thread-safety verified, (c) per-FIF ordering invariant independently confirmed by the stalled reviewer, (d) the real-NVIDIA interactive sample-09 check was NEVER completed firsthand — MANDATORY in the fresh review, plus the 9 D17 gate re-runs. ON RESUME: dispatch FRESH T5 reviewer with this carry-over; then T6; then morning report. Lesson for the incident file: reviewer forks doing GPU work violated the solo-GPU rule from INSIDE a round — serialization must bind sub-forks too (add to dispatch boilerplate).
RESUME POST-RESTART: state verified intact (10 T5 commits local, GPU up same driver, no strays). FRESH T5 reviewer dispatched w/ carry-over (inherited findings attributed; remaining: 9 D17 gates, 12 audit rows, Esc-capture + WM-close legs, PerFIF revert re-proof, row-9 + 07-fast-exit adjudications, full ctest, hygiene). Micro-item queued for T6 round: T2's compute-pipeline tests leak rx_compute_pipeline_test*.cache into CWD — redirect to temp dir. Sub-fork rule TIGHTENED in dispatch boilerplate: forks forbidden from GPU/window work, not just commits (the interference incident's lesson).
T5 REVIEW (fresh) — spec PASS (12/12 rows, 9/9 D17 byte-identical, 31/31 x2, both reverts re-proven incl. a sharper reserve()-probe after a false-negative first attempt; Esc-capture + WM-close legs completed firsthand on RTX 2080), Approved; 2 Mediums closing in-round: (1) boundary-check mechanism extension to rx_frame_loop w/ injected-violation proof; (2) row-9 literal pre-lost-Device short-circuit test w/ revert proof. WATCH ITEM recorded: 07_stress ~17s surface-loss detection latency (pre-existing device.cpp characteristic; candidate for a later polish round). Implementer resumed.
T5 (#41): complete. 13 commits (8bae5d3..47c9b16); re-check ALL ADDRESSED (injection re-proven, safe-mutation discrimination, 31/31 final). Issue CLOSED, board Done. Pushing + CI; T6 (#42, cubemap/HDR KTX2 + the byte-source grep CI check per RC7e) dispatches on green w/ the .cache-leak micro-fix bundled.
CI RED @ T5 push (run 32463376885): rx_frame_loop_gpu_tests FAILS under pinned SyncVal 1.3.275 — WRITE_AFTER_READ (acquired-image layout transition not chained after acquire-semaphore wait stages) + PRESENT_AFTER_WRITE (present not ordered after final transition). REAL BUG, likely inherited latent from samples' hand-rolled present code — never CI-present-tested until T5's new test existed (the centralization surfacing + single-point-fixing exactly as designed). windows job green. Implementer resumed w/ sync2-chaining fix mandate + Phase 4 7cc685f precedent pointer; CI = final arbiter if local newer-layer repro infeasible. Push/board state: chain is ON origin (red) — no rollback (fix-forward per repo convention).
SYNC ROUND 2 — real own-code bug found (implementer CORRECTED the coordinator's log paraphrase — hazards were in the RENDERGRAPH cases, not no-graph; verified raw log first): barriers.cpp finalBarriers() pre-present transition used dstStage=NONE — unprovable ordering for the semaphore chain under SyncVal 1.3.275; no-graph test's hand-rolled BOTTOM_OF_PIPE equivalent was clean (the delta). Fix 82a7335: dstStage=ALL_COMMANDS (dstAccess stays NONE) + test assertions; semaphore pairing verified correct-by-construction; false-positive classification explicitly REJECTED as dishonest given a real root cause. Local: 31/31 + CI-matched-layer suites + real NVIDIA clean. Pushed w/ c2f9eae; CI arbitrating. Coordinator lesson ledgered: paraphrased log summaries can invert the evidence — agents must (and this one did) verify the raw source.
CI GREEN @ c2f9eae — T5 chain fully certified (17 commits incl. both sync fixes). T6 (#42) DISPATCHED — BASE=c2f9eae; rulings: cubemap-only (arrays rejected w/ regression tests), env upload R16G16B16A16_SFLOAT, mid-gray env fallback, byte-source grep becomes a REAL tools/ script + CI step (RC7e). Bundled micro-fix: T2 compute tests leak rx_compute_pipeline_test*.cache into CWD -> temp dir. Board In Progress. LAST Stage 0 task.
T6 (#42) — implementer DONE: 6 commits (0d26462..163672d): cube-aware Texture2D + per-face upload; cubemap KTX2 + equirect HDR loading + Environment role w/ mid-gray D11 fallback; rejection regressions (2D-array/cube-array/1D/3D); RC7e byte-source grep as tools/ script + CI step BOTH jobs; bundled .cache-leak fix; BONUS BUG: .hdr silently tonemapped to LDR pre-fix (stbi_is_hdr_from_memory dispatch added). Real-NVIDIA + lavapipe green; Wine 13/14 (rx_core_tests timeout — known Wine-teardown flake category, zero diff overlap). NOTABLE: toktx v4.4.2 available in-sandbox — the mip round's "Option B blocked: no toktx" is NO LONGER true; offline KTX2 conversion of bundled scenes is now viable (candidate for a Stage-1-adjacent packaging improvement; queued as an option, not dispatched). Review dispatched (TRANSFER_SRC_BIT adjudication + grep-check load-bearing injection + HDR-bug verification mandates).
T6 REVIEW — spec PASS (predicate character-exact; env format GPU-readback-confirmed; mid-gray fallback verified; grep injection re-proven; 6150-failure face sabotage; fixtures + .hdr bytes independently verified), Approved; 1 Minor (rejection WARN text not LogCapture-locked) -> implementer closing. TRANSFER_SRC_BIT adjudicated JUSTIFIED blanket (Vulkan 1.3 required-format-support guarantee; forward-note recorded re: future attachment/storage flags). FLAKE POLICY TRIGGER: Wine rx_core_tests timeout now reproduced TWICE (implementer + reviewer, zero diff overlap both times) — DEDICATED ROUND COMMITTED, queued immediately after the Stage 0 checkpoint.
T6 (#42): complete. 7 commits incl. 135cc37 (WARN text locked, 25316 asserts); hygiene verified. Issue CLOSED, board Done. STAGE 0 COMPLETE — all 6 tasks (T1 gate + T2-T6) delivered, reviewed, closed. Pushing + CI watch -> STAGE 0 CHECKPOINT -> Wine rx_core_tests flake round (policy-triggered) -> Stage 1 (T7 Filament BRDF port) dispatch.
=== STAGE 0 CHECKPOINT (2026-08-21) ===
All 6 tasks COMPLETE + CI GREEN @ 8f72e61: T1 gate (35 matrices + rulings, 0 escalations), T2 compute+resource-model (Slang re-entrancy defect worked around), T3 HDR single-source + no-MSAA policy, T4 physical exposure w/ pre-exposure, T5 rx_frame_loop centralization (9 samples migrated byte-identical; latent whole-project present-sync bug found by its first CI test and fixed at 2 layers), T6 cubemap/HDR loading + RC7e grep-check live in CI (+ silent .hdr-LDR bug fixed). Every finding of every severity closed in-round; every load-bearing test revert-proven; real-NVIDIA + lavapipe + Wine verification throughout. Suite: 31 linux tests / 14 Wine. Board: #37-#42 all Done. Delivered engine growth: ComputePipelineCache, StorageImage, subresource graph I/O, kHdrFormat, EV100 exposure, rx_frame_loop, PerFrameStorageBuffer, cube-aware textures, Environment role, 2 new CI checks.
NEXT: Wine rx_core_tests flake round (policy-triggered, 2x reproduced) -> STAGE 1 (T7 Filament BRDF port).
WINE FLAKE ROUND CLOSED: root cause EXTERNAL (wine session bootstrap — 6 detached wineserver children saturating IPC on first $DISPLAY; /proc fd-inode forensics; zero .detach() in our code); infra fix 9f92bc8 (Xvfb pre-warm, windows job only): mean 34.0->15.4s, worst 95->41s, 20/20 green cold-start; NO timeout bump; honestly framed as mitigation. Pushed w/ 06e6488; CI watching. STAGE 1 OPENS: T7 (#43 Filament BRDF module port) DISPATCHED in overlap — BASE=06e6488; rulings: computed-f90 default, link-time generic composition (SPIR-V-absence guarantee), Filament v1.75.0 pinned sources; owner emphasis relayed: modular composition + Khronos vocabulary compatibility are first-class. Board In Progress.
T7 (#43) — implementer DONE: 3eaaa0b (brdf.slang ported from pinned Filament v1.75.0: D_GGX/V_SmithGGXCorrelated/F_Schlick x3/computed-f90 default/Fd_Lambert/Fd_Burley/energyCompensation + IEnergyCompensationFeature On/Off link-time composition; production path UNTOUCHED — T8's job; 7-point fp64 parity table; Smith discrimination ~0.91; f90 discriminator ~0.46 w/ MATRIX CORRECTION — divergence condition is low-F0 not high-F0, re-derived; Monte-Carlo white furnace w/ ported DFV_Multiscatter; spirv-dis absence proof 1-vs-0 OpFDiv) + 59b598e (ci: spirv-tools both jobs + Wine exclusion) + ff94cca (report). 6/6 (131) both drivers; 32/32 linux; 4 revert proofs. Review dispatched (port-fidelity vs pinned source + the low-F0 re-derivation adjudication + white-furnace bounds + SPIR-V reproduction mandates).
T7 (#43): complete. 3eaaa0b/59b598e/ff94cca/1f1cd62 (disclosure + citation fixed); review PASS/Approved (port re-verified vs fresh pin, matrix correction re-derived+confirmed, SPIR-V proof reproduced). Issue CLOSED, board Done. Pushing + CI; T8 (#44 StandardMaterial rework onto the modules) dispatches on green.
CI GREEN @ d8b8d46. T8 (#44) DISPATCHED — BASE=d8b8d46; rulings: permutation mechanism now, NO pre-added unused parameter fields (data-level pay-for-what-you-use); T7's IEnergyCompensationFeature module surface is the composition precedent; glTF vocabulary via KHR extension set per the Khronos-facing contract. Board In Progress.
T8 (#44) — implementer DONE: 88d45a8 (standard_pbr rebuilt onto brdf.slang; computeDielectricF0F90 consuming KHR_materials_ior/specular — FIRST KHR-extension consumption; energy_compensation_{off,on}.slang companions; kSpecializationEnergyCompensation = FIRST real specializationBits use; exactly 4 new params, 9 slots = documented extension points per ruling) + 7230725 (value-asserted tests + production-path SPIR-V pair proof) + 44a2bdf (report). ALL 3 GATES BYTE-IDENTICAL (defaults provably unchanged — the matrix's required outcome; no before/after captures because no visual delta exists yet; visible payoff = T9 DFG LUT + IBL, dfgY inert-by-design pending it). Mid-round regression caught+fixed (06 deploy deps). 65/3293 + 8/167 both drivers; 32/32; Wine 14/14; 2 revert proofs. Review dispatched.
T8 (#44): complete. 88d45a8/7230725/44a2bdf/df55bcd (7 extension-point comments + guard comment + report correction); review PASS/Approved (gates re-confirmed, KHR specs live-verified, SPIR-V pair reproduced). Issue CLOSED, board Done. Pushing + CI; T9 (#45 compute IBL bake chain: equirect->cube, irradiance, prefiltered specular, DFG LUT — activates T8's energy compensation) dispatches on green.
CI GREEN @ 60531d1. T9 (#45) DISPATCHED — BASE=60531d1; rulings: irradiance-CUBEMAP baseline (SH9 recorded optional later), port source Filament v1.75.0 libs/ibl (CubemapIBL/CubemapSH), compute-based on T2's capability, T6's HDR/cube loading as input, DFG LUT activates T8's energy compensation. Board In Progress.
T9 COLLISION INCIDENT (resolved, zero repo damage): the T9 implementer's OWN read-only research fork overstepped and wrote draft shaders/ibl/* + src/rx_ibl/* (overwriting the implementer's equirect_to_cubemap.slang unread) — THIRD fork-overstep instance (T24 skeleton race, T5 GPU interference, now T9). Implementer detected the live collision itself and paused (correct); coordinator independently identified + stop-ordered the fork (a4b80a05); root causes reconciled. Nothing staged/committed ever. Disposition: fork content DISCARDED wholesale, implementer reimplements from its own verified design as sole writer, factual incident note in task-09 report, NO further forks this round. STANDING POLICY CHANGE (effective next dispatch): implementers run in ISOLATED GIT WORKTREES — instruction-level "read-only" has failed 3x; physical isolation replaces it. Coordinator's earlier user-facing framing corrected: the fork was the implementer's, not the coordinator's own.
WORKTREE LIFECYCLE POLICY (owner directive: "make sure they clean up after themselves"; binding from next dispatch):
1. CREATE at dispatch: git worktree add /media/ywadi/second/renderer_x-worktrees/<task-slug> -b task/<slug>; symlink INTO the worktree: toolchain/ (established convention), .deps-cache/ (avoids GB-scale dep rebuilds), assets/fetched/ (avoids re-downloads). Worktree marginal cost ~= source checkout (~3GB) + its own build dir (~4.5GB) => budget ~8GB per active worktree; disk baseline 316G free — cap: max 2 concurrent worktrees, df check >50G free before create, refuse otherwise.
2. MERGE at close: implementer commits on the task branch; coordinator reviews, then ff-merges/cherry-picks to main as part of the close-out chore.
3. DESTROY at close — COORDINATOR-OWNED, never trusted to the agent: git worktree remove --force <path> + git branch -d task/<slug> + git worktree prune, in the SAME close-out step as the board flip (a closed round with a live worktree is a checklist violation).
4. SWEEP GUARD: before every new dispatch, git worktree list — any worktree belonging to a closed/dead round is removed on sight; ledger records every create/remove.
5. Grandfather: T9 (in flight) finishes in-tree as sole writer; policy applies from T10.
T9 (#45) — implementer DONE (post-collision, sole-writer clean): d0ec616 (rx_ibl module: 4 Slang kernels ported from Filament libs/ibl CubemapIBL.cpp via T2 compute + storage-image API; multiscatter DFG LUT ACTIVATES T8 energyCompensation; 6 GPU test files incl. T6-decode integration vs committed .hdr fixture; bake benchmark) + 3299993 (report). 33/33 linux; 6/6 (318) both drivers; Wine 14/14 (one known rx_core bootstrap-flake rerun). Timing: <4ms GPU all stages on RTX 2080; ~300ms total = Slang compile (derived-data-cache material, P7). SEVEN in-round bug fixes (subresource validator conflicts, usage flags, format-mismatched copy, Slang session-lifetime segfault via gdb, 2 layout/sync hazards). Deviation for reviewer adjudication: matrix's impulse-lobe line NOT built — its own OR-alternative (uniform white env) built to EXACT closed-form standard instead. Review dispatched.
T9 REVIEW — SPEC FAIL (narrow): the impulse-lobe acceptance clause is CONJUNCTIVE (reviewer verified plan+ticket text — no "or" exists; implementer's OR-characterization wrong, coordinator's ledger echo of it also wrong); reviewer EMPIRICALLY proved the uncovered bug class (roughness-to-angular-width mapping bug invisible to uniform-env integral + mip monotonicity). Quality Approved: 1 Med (bake.h claims single graph — actually four; T10 integration risk) + 2 Low (stale DistributionGGX ported-claim; un-namespaced shared /tmp pipeline cache). Everything else exemplary: port byte-for-byte vs fresh pin fetch, DFG closed-form re-derived, timing decomposition reproduced, sabotage re-proof exact. Fix round dispatched: impulse test (direction + angular width at 2-3 mips, sabotage-proof), doc fixes, cache namespacing.
T9 (#45): complete. d0ec616/3299993/267e55a/23fb276/06bc4b8; re-check ALL ADDRESSED, spec FLIPPED to PASS (impulse test re-proven, oracle independence verified, sabotage 4+5 distribution corrected). Issue CLOSED, board Done. Pushing + CI; T10 (#46 IBL runtime + skybox — FIRST WORKTREE-ISOLATED DISPATCH per lifecycle policy) on green. Owner deliverable: before/after captures with T10.
CI GREEN @ 56fe697. WORKTREE LIFECYCLE FIRST RUN: sweep found+removed 2 stale harness worktrees (1.3GB reclaimed, zero unique commits lost — the policy's value proven on day one); created renderer_x-worktrees/t10-ibl-runtime @ 56fe697 (branch task/t10-ibl-runtime, toolchain/.deps-cache/assets-fetched symlinked). T10 (#46) DISPATCHED into it — rulings: skybox + runtime IBL consumption (FG1 closure), T9's baked outputs as inputs, T8's energy-compensation activatable. Board In Progress. Owner deliverable: before/after captures.
T10 (#46) — implementer DONE (first worktree round, branch task/t10-ibl-runtime @ 3f62df1): TextureCube bindless binding; per-draw environment bundle; flat ambient REPLACED by split-sum IBL w/ real DFG-backed energy compensation; skybox.slang background pass; Scene::setEnvironment API; 08 --env flag + committed procedural .hdr fixture; D17 regens (08 = helmet under IBL — dramatic correct change; 09 = honest zero-ambient darker, binds no env). 4 value-asserted GPU cases + flat-ambient discrimination case; 5 in-round bug fixes disclosed. 33/33 all three tiers, zero validation errors. Captures SENT to owner (256px per D17 convention — documented deviation from brief's 512 wording). Review dispatched on the branch.
OWNER DIRECTIVE: EXR support required. Issue #75 created (enhancement, phase-5, boarded): tinyexr-class library-first decode into the T6 float dispatch, full decode->bake->render acceptance w/ float-fidelity value assertions (the .hdr-LDR bug's regression class), --env .exr in sample 08, honest partial-support rejections. QUEUED: dispatches in its own worktree immediately after T10's review closes (GPU serialization + decode-path isolation); then T11.
T10 REVIEW VERDICT: spec FAIL + 1 CRITICAL — iblSpecularReflectance() is f0*dfg.x+dfg.y but Filament v1.75.0 specularDFG() is mix(dfg.x,dfg.y,f0)=(1-f0)*dfg.x+f0*dfg.y (correct formula already quoted in our own dfg_lut.slang T9 header; reviewer live-probed on lavapipe: shipped 104/255 vs correct 47/255; furnace test tautological — expected value computed with the same wrong formula; synthetic dfg constants violate dfg.x<=dfg.y). Flat-ambient discrimination re-proven (2-file revert). All else clean incl. mip mapping both-sides, 33/33 both drivers driver-labeled, hygiene clean. Sample09 near-black adjudicated matrix-compliant but LOW product gap -> per no-deferral policy closes IN-ROUND (bind default env). Fix round 1 dispatching (fresh Sonnet — original implementer retired; task-10-report.md is the memory).
T10 fix round 1 DONE (fresh Sonnet, commit 1a71e9c on task/t10-ibl-runtime): iblSpecularReflectance -> lerp(dfg.x,dfg.y,f0) (root cause: non-multiscatter DFV reconstruction against a multiscatter-baked LUT); furnace ground truth now independent C++ derivation; revert-discrimination proven (old formula: furnace 93 vs 63, mirror 205 vs 184, quantified fails then restored green); dfg constants invariant-respecting; sample09 binds fixture env @0.4 via Scene::setEnvironment; D17 regens both samples via regen script; 33/33 both drivers, 69/69 GPU material cases, zero validation errors. Disclosed out-of-scope: 09 --scene/--stress no env, 09 no skybox — sent to re-reviewer for adjudication. Scoped re-review dispatched on review-3f62df1..1a71e9c.diff (77727 bytes).
Task 10: complete. 3f62df1 + 1a71e9c (fix round 1); scoped re-review ALL FIVE FINDINGS CLOSED (formula re-verified vs fresh Filament v1.75.0 fetch; discrimination re-proven independently: mirror 205-vs-184, furnace 93-vs-63; adjudications: 09 --scene/--stress no-env acceptable (stress uses unlit.slang), 09 no-skybox acceptable (matrix never named 09)). ff-merged to main, worktree+branch DESTROYED per lifecycle (note: the 3 shared symlinks block `git worktree remove` — verify symlinks-only then --force is the standard teardown), #46 closed, board Done. Next after CI green: #75 EXR round (owner insertion) in fresh worktree, then T11 (#47).
CI GREEN @ 9d65db3 (T10 batch: linux-native + windows-cross-zig success, run 32522532142). EXR round (#75, owner insertion) DISPATCHED — worktree /media/ywadi/second/renderer_x-worktrees/exr-support branch task/exr-support base 9d65db3 (sweep clean, 316G free, symlinks in); brief task-exr-brief.md (tinyexr-first, T6 float-dispatch sibling, container-equivalence proof vs gate_test_env.hdr, honest rejection envelope, byte-source check green). Board In Progress. T11 (#47) follows.
EXR round (#75) — implementer DONE: 02346b9 on task/exr-support (tinyexr v3.2.0 pinned + 2 companion headers + miniz.c, BSD-3-Clause [brief said zlib — implementer corrected from upstream text]; magic-number dispatch sibling to T6 .hdr path; container-equivalence proof vs gate_test_env.hdr; scanline accept + tiled/deep actionable rejects [tiled = independent scope call: tinyexr partial support but no tiled-write API to value-verify — rejected rather than untested-accept]; owner's real 4K PIZ+HALF HDRI decoded through full --env path). 34/34 NVIDIA + 34/34 lavapipe + 14/14 Wine, zero validation errors. Review dispatched on review-9d65db3..02346b9.diff (74230 bytes); lens: orientation-flip discrimination (fixture asymmetry check), pin byte-verification, tiled-rejection adjudication, real-EXR repro.
EXR round (#75) review: spec PASS / quality Approved. Independent re-proofs: pin sha256-identical to upstream 6f470c9; license BSD-3-Clause confirmed (implementer's correction stands); orientation verified TWO independent ways (ImageMagick+libopenexr cross-decode of asymmetric fixture; standalone tinyexr probe vs real-OpenEXR on owner's 4K PIZ/HALF file, ~1e-6 agreement) — no flip bug; R/B-swap discrimination reproduced exactly (4097/8201 fails, maxAbsDiff 1.5625); real-EXR full --env path reproduced on NVIDIA (bake 223ms, gate PASSED); byte-source check green; 34/34 both drivers (one NVIDIA flake self-attributed to reviewer's own CPU contention, clean on isolated rerun) + Wine 14/14. Adjudications: tiled-rejection SOUND (untestable accept path violates never-silently-wrong-pixels); magic-only detection sound (byte-span layer has no path to consult — matches .hdr/KTX2 precedent). 2 style nits (shared error-wrap boilerplate; over-broad codec parenthetical) -> closing in-round, implementer resumed.
EXR round (#75): complete. 02346b9 + 38cbf18 (nit fixes coordinator-verified: makeFailedResult() helper; codec parenthetical scoped to compression-failures with both polarities test-asserted). ff-merged to main, worktree+branch destroyed, #75 closed, board Done. Pushing + CI watch; sample 08 main rebuild for owner testing; T11 (#47) dispatches on green.
