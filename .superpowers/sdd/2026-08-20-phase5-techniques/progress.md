# SDD ledger — plan: docs/superpowers/plans/2026-08-20-phase5-techniques.md

PHASE 5 (TECHNIQUES) SDD LEDGER. Board tickets #37-#72 = P5 T01-T36. Owner decision policy (2026-08-20, standing): coordinator takes the best-recommended option autonomously; ONLY genuinely unclear/owner-level decisions escalate. Inherited standing rules: no-deferral (incl. pre-existing defects), real-GPU verification alongside lavapipe (driver-labeled), decoded/rendered-VALUE assertions (never import-success-only), revert-discrimination proofs, mirror tickets at dispatch, pathspec commits, no AI attribution, CI watched per push, cd -P real path, samples consume engine/shared facilities (present-loop centralization committed in T5/#41).

P5 T1 PRIMARY GATE (#37) KICKED OFF: 5 parallel read-only stage-research agents -> per-ticket completeness matrices in gate/; coordinator authors binding rulings after; plan+matrices+rulings order of authority per phase-4 precedent (rulings > plan > matrix > ticket).
STANDING RULE (owner, forceful — binding on all P5 dispatches): on-desktop/GPU-interactive verification is SERIALIZED (machine to itself), ANNOUNCED to owner beforehand, agent builds run NICEd, WM-semantics trials prefer a nested Xephyr display. Memory: feedback_desktop_verification.md. Origin incident: #74 trials + concurrent builds froze the owner's desktop.
OVERNIGHT MANDATE #2 (owner, 2026-08-21): CMake upgrade CANCELLED (no toolchain revalidation needed). Once all fixes land properly and clean: PROCEED AUTONOMOUSLY WITH PHASE 5. Sequence: #74 LOW fix -> verify -> close #74 -> push batch (close-race + gate + erratum + chores) -> CI green -> release v5 rebuild + asset swap (owner's refreshed test build) -> close #37 gate -> STAGE 0 serial task loop (T2 compute -> T3 HDR/MSAA -> T4 exposure -> T5 audit/centralization -> T6 KTX2-cube) under full discipline: mirror-ticket status flips at dispatch, matrix+rulings-driven briefs, independent reviews, in-round closures incl. pre-existing discoveries, real-GPU driver-labeled verification, desktop-trial serialization (owner asleep — still solo + brisk), NICEd builds, CI watch per push, decision policy = best-recommended autonomously. Morning report: release link, stage progress, incidents, anything owner-level.
CI GREEN @ 1c01881 (full batch: close-race + gate + erratum). P5 T1 (#37) CLOSED, board Done — gate artifacts pushed. Release v5 rebuild dispatched (serialized: no other agent work during its GPU trials); on swap completion: Stage 0 T2 (#38) dispatches per rulings (parallel ComputePipeline facility in rx_rhi_vk + RC2 resource-model widening).
RELEASE v5 SWAP in flight (assets verified incl. packaged-binary close trials w/ live suppression telemetry). STAGE 0 IMPLEMENTATION OPENS: T2 (#38, compute pipeline capability) DISPATCHED — BASE=1c01881, authority: rulings RC2 + T2 per-ticket > plan Task 2 > matrix-p5t02 > ticket. Board In Progress.
RELEASE v5 LIVE (assets+body swapped, sizes verified). Phase 4 fully final for owner testing: upright helmets, resize+F11, mouse capture, clean close (fix telemetry-proven in the packaged binary), sponza+workshop bundled, draco proven. T2 (#38) implementer running.
T2 (#38) — implementer DONE: 00bb451 (ComputePipelineCache sibling facility w/ own persisted VkPipelineCache + content-hash key; StorageImage mip/array/cube w/ on-demand subresource views; RC2 graph extension: Subresource/ImageDesc, addStorageImage{Out,In}put, per-(resource,subresource) barriers, executor/transient wiring, PassContext::storageImageView; value-asserted compute GPU tests incl. cube-face independence; attachment-free-rejection gap closed). 29/29 lavapipe + Wine 13/13 + REAL NVIDIA 162/162, zero validation errors. KEY FINDING: vendored Slang 2026.14.1 re-entrancy defect (2nd getLayout() SIGFPE/SIGSEGV for compute-only linked programs post-Vulkan-init) — worked around in rx_shader (reuse compileImpl's layout), revert-proven; upstream-flag noted for future Slang bumps. Scope boundary documented: subresource validator = identical-or-disjoint only. Review dispatched.
T2 (#38): complete. 00bb451 + b73c563 (mip-axis overlap tests, 50/358 device-free); review PASS/Approved, all closed. Issue CLOSED, board Done. Pushing + CI watch; T3 (#39 HDR scene-color + FG6 no-MSAA ruling) dispatches on green.
CI GREEN @ 9f7dfb2. T3 (#39) DISPATCHED — BASE=9f7dfb2; rulings: B10G11R11 process default + A16F escape hatch, FG6 = no-MSAA/TAA-first documented rejection, kHdrFormat single-sourced in engine. Board In Progress.
T3 (#39) — implementer DONE: e341433 (scene_color.h single source: kHdrFormat B10G11R11 + A16F escape hatch + consumer table + FG6 no-MSAA documented rejection w/ named structural gaps; 4 samples migrated off local copies, grep-verified zero remain; test_scene_color_gpu 4 cases w/ UNORM8-discrimination mutant + honest blue-mantissa proof R=G=4.3125/B=4.25; D17 gates byte-identical post-switch — no regen needed). 29/29 lavapipe + Wine 13/13 + real NVIDIA 29/29, zero validation errors. Notables: doctest INFO stringification quirk isolated+worked-around; storage-image support for kHdrFormat queried true both drivers, transitive-assumption warning recorded for RADV/Deck. Review dispatched.
T3 (#39): complete. e341433 + 154e78c (comment pointers fixed); review PASS/Approved (mantissa re-derived, mutation self-applied, D17 re-confirmed). Issue CLOSED, board Done. Pushing + CI; T4 (#40 camera exposure/physical units, pre-exposure ruling) dispatches on green.
