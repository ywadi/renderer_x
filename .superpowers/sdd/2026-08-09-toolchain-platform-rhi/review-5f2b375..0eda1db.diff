# Review package: 5f2b375157763115ce21dd6e1b6fe9416ec6995a..0eda1db0f99b4046995146119774fa70ecdddf49

## Commits
0eda1db Add rx_core: logging, generational handles, GLM math

## Files changed
 CMakeLists.txt                       |  1 +
 src/rx_core/CMakeLists.txt           | 14 +++++++
 src/rx_core/include/rx_core/handle.h | 74 ++++++++++++++++++++++++++++++++++++
 src/rx_core/include/rx_core/log.h    | 12 ++++++
 src/rx_core/src/log.cpp              | 14 +++++++
 src/rx_core/tests/doctest_main.cpp   |  2 +
 src/rx_core/tests/handle_test.cpp    | 21 ++++++++++
 src/rx_core/tests/log_test.cpp       | 26 +++++++++++++
 src/rx_core/tests/math_test.cpp      | 11 ++++++
 third_party/CMakeLists.txt           | 16 ++++++++
 10 files changed, 191 insertions(+)

## Diff
diff --git a/CMakeLists.txt b/CMakeLists.txt
index 9ef6df8..635737e 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -1,12 +1,13 @@
 cmake_minimum_required(VERSION 3.21)
 project(renderer_x LANGUAGES C CXX)
 
 set(CMAKE_CXX_STANDARD 20)
 set(CMAKE_CXX_STANDARD_REQUIRED ON)
 set(CMAKE_EXPORT_COMPILE_COMMANDS ON)
 
 enable_testing()
 
 add_subdirectory(third_party)
+add_subdirectory(src/rx_core)
 add_subdirectory(tools/dep_cache_smoketest)
 add_subdirectory(tools/toolchain_check)
diff --git a/src/rx_core/CMakeLists.txt b/src/rx_core/CMakeLists.txt
new file mode 100644
index 0000000..88a9438
--- /dev/null
+++ b/src/rx_core/CMakeLists.txt
@@ -0,0 +1,14 @@
+add_library(rx_core STATIC
+    src/log.cpp
+)
+target_include_directories(rx_core PUBLIC include)
+target_link_libraries(rx_core PUBLIC spdlog::spdlog glm::glm)
+
+add_executable(rx_core_tests
+    tests/doctest_main.cpp
+    tests/log_test.cpp
+    tests/handle_test.cpp
+    tests/math_test.cpp
+)
+target_link_libraries(rx_core_tests PRIVATE rx_core doctest::doctest)
+add_test(NAME rx_core_tests COMMAND rx_core_tests)
diff --git a/src/rx_core/include/rx_core/handle.h b/src/rx_core/include/rx_core/handle.h
new file mode 100644
index 0000000..22e6cda
--- /dev/null
+++ b/src/rx_core/include/rx_core/handle.h
@@ -0,0 +1,74 @@
+#pragma once
+#include <cstdint>
+#include <vector>
+
+namespace rx::core {
+
+template <typename Tag>
+class Handle {
+public:
+    Handle() = default;
+    Handle(uint32_t index, uint32_t generation) : index_(index), generation_(generation) {}
+
+    uint32_t index() const { return index_; }
+    uint32_t generation() const { return generation_; }
+    bool isValid() const { return generation_ != 0; }
+
+    bool operator==(const Handle& other) const {
+        return index_ == other.index_ && generation_ == other.generation_;
+    }
+
+private:
+    uint32_t index_ = 0;
+    uint32_t generation_ = 0;
+};
+
+template <typename Tag, typename T>
+class HandlePool {
+public:
+    Handle<Tag> acquire(T value) {
+        if (!freeList_.empty()) {
+            uint32_t idx = freeList_.back();
+            freeList_.pop_back();
+            slots_[idx].value = std::move(value);
+            slots_[idx].generation += 1;
+            slots_[idx].alive = true;
+            return Handle<Tag>(idx, slots_[idx].generation);
+        }
+        slots_.push_back(Slot{std::move(value), /*generation=*/1, /*alive=*/true});
+        return Handle<Tag>(static_cast<uint32_t>(slots_.size() - 1), 1);
+    }
+
+    void release(Handle<Tag> handle) {
+        if (!isLive(handle)) {
+            return;
+        }
+        slots_[handle.index()].alive = false;
+        freeList_.push_back(handle.index());
+    }
+
+    T* get(Handle<Tag> handle) {
+        if (!isLive(handle)) {
+            return nullptr;
+        }
+        return &slots_[handle.index()].value;
+    }
+
+private:
+    struct Slot {
+        T value;
+        uint32_t generation = 0;
+        bool alive = false;
+    };
+
+    bool isLive(Handle<Tag> handle) const {
+        return handle.index() < slots_.size() &&
+               slots_[handle.index()].alive &&
+               slots_[handle.index()].generation == handle.generation();
+    }
+
+    std::vector<Slot> slots_;
+    std::vector<uint32_t> freeList_;
+};
+
+}  // namespace rx::core
diff --git a/src/rx_core/include/rx_core/log.h b/src/rx_core/include/rx_core/log.h
new file mode 100644
index 0000000..58fdc2e
--- /dev/null
+++ b/src/rx_core/include/rx_core/log.h
@@ -0,0 +1,12 @@
+#pragma once
+#include <spdlog/spdlog.h>
+
+namespace rx::core::log {
+
+void init();
+
+}  // namespace rx::core::log
+
+#define RX_LOG_INFO(...)  ::spdlog::info(__VA_ARGS__)
+#define RX_LOG_WARN(...)  ::spdlog::warn(__VA_ARGS__)
+#define RX_LOG_ERROR(...) ::spdlog::error(__VA_ARGS__)
diff --git a/src/rx_core/src/log.cpp b/src/rx_core/src/log.cpp
new file mode 100644
index 0000000..70163f8
--- /dev/null
+++ b/src/rx_core/src/log.cpp
@@ -0,0 +1,14 @@
+#include <rx_core/log.h>
+
+namespace rx::core::log {
+
+void init() {
+    static bool initialized = false;
+    if (initialized) {
+        return;
+    }
+    spdlog::set_pattern("[%Y-%m-%d %H:%M:%S.%e] [%^%l%$] %v");
+    initialized = true;
+}
+
+}  // namespace rx::core::log
diff --git a/src/rx_core/tests/doctest_main.cpp b/src/rx_core/tests/doctest_main.cpp
new file mode 100644
index 0000000..0a3f254
--- /dev/null
+++ b/src/rx_core/tests/doctest_main.cpp
@@ -0,0 +1,2 @@
+#define DOCTEST_CONFIG_IMPLEMENT_WITH_MAIN
+#include <doctest/doctest.h>
diff --git a/src/rx_core/tests/handle_test.cpp b/src/rx_core/tests/handle_test.cpp
new file mode 100644
index 0000000..c526a52
--- /dev/null
+++ b/src/rx_core/tests/handle_test.cpp
@@ -0,0 +1,21 @@
+#include <doctest/doctest.h>
+#include <rx_core/handle.h>
+
+struct MeshTag {};
+
+TEST_CASE("HandlePool acquire/get/release round-trips a value and invalidates stale handles") {
+    rx::core::HandlePool<MeshTag, int> pool;
+
+    auto h1 = pool.acquire(42);
+    CHECK(h1.isValid());
+    CHECK(*pool.get(h1) == 42);
+
+    pool.release(h1);
+    CHECK(pool.get(h1) == nullptr);
+
+    auto h2 = pool.acquire(7);
+    CHECK(h2.isValid());
+    CHECK(*pool.get(h2) == 7);
+    CHECK(h1.index() == h2.index());
+    CHECK(h1.generation() != h2.generation());
+}
diff --git a/src/rx_core/tests/log_test.cpp b/src/rx_core/tests/log_test.cpp
new file mode 100644
index 0000000..515fb76
--- /dev/null
+++ b/src/rx_core/tests/log_test.cpp
@@ -0,0 +1,26 @@
+#include <doctest/doctest.h>
+#include <rx_core/log.h>
+#include <spdlog/sinks/ostream_sink.h>
+#include <sstream>
+
+TEST_CASE("log::init is idempotent") {
+    rx::core::log::init();
+    rx::core::log::init();
+    CHECK(true);
+}
+
+TEST_CASE("RX_LOG_INFO writes the formatted message through spdlog's default logger") {
+    rx::core::log::init();
+    auto previousDefault = spdlog::default_logger();
+
+    std::ostringstream capture;
+    auto sink = std::make_shared<spdlog::sinks::ostream_sink_mt>(capture);
+    auto testLogger = std::make_shared<spdlog::logger>("test", sink);
+    testLogger->set_pattern("%v");
+    spdlog::set_default_logger(testLogger);
+
+    RX_LOG_INFO("hello {}", 42);
+
+    spdlog::set_default_logger(previousDefault);
+    CHECK(capture.str() == "hello 42\n");
+}
diff --git a/src/rx_core/tests/math_test.cpp b/src/rx_core/tests/math_test.cpp
new file mode 100644
index 0000000..e7dd24d
--- /dev/null
+++ b/src/rx_core/tests/math_test.cpp
@@ -0,0 +1,11 @@
+#include <doctest/doctest.h>
+#include <glm/glm.hpp>
+
+TEST_CASE("GLM is linked and usable") {
+    glm::vec3 a{1.0f, 2.0f, 3.0f};
+    glm::vec3 b{4.0f, 5.0f, 6.0f};
+    glm::vec3 c = a + b;
+    CHECK(c.x == doctest::Approx(5.0f));
+    CHECK(c.y == doctest::Approx(7.0f));
+    CHECK(c.z == doctest::Approx(9.0f));
+}
diff --git a/third_party/CMakeLists.txt b/third_party/CMakeLists.txt
index 6926f52..85f9d39 100644
--- a/third_party/CMakeLists.txt
+++ b/third_party/CMakeLists.txt
@@ -8,10 +8,26 @@ rx_add_cached_dependency(
   CMAKE_ARGS -DSPDLOG_BUILD_EXAMPLE=OFF -DSPDLOG_BUILD_TESTS=OFF -DBUILD_SHARED_LIBS=OFF
 )
 find_package(spdlog REQUIRED PATHS "${spdlog_CACHE_DIR}" NO_DEFAULT_PATH)
 
 # CMake 3.22 has no find_package(... GLOBAL) (that arrived in 3.24), and
 # imported targets are directory-scoped by default: without this, the
 # spdlog::spdlog target created here would be invisible to sibling
 # add_subdirectory() calls (e.g. tools/dep_cache_smoketest) that need to
 # link against it. Promote it to global scope explicitly instead.
 set_target_properties(spdlog::spdlog PROPERTIES IMPORTED_GLOBAL TRUE)
+
+include(FetchContent)
+
+set(RX_DOCTEST_TAG "v2.5.3")
+FetchContent_Declare(doctest
+  GIT_REPOSITORY https://github.com/doctest/doctest.git
+  GIT_TAG ${RX_DOCTEST_TAG}
+  GIT_SHALLOW TRUE)
+FetchContent_MakeAvailable(doctest)
+
+set(RX_GLM_TAG "1.0.3")
+FetchContent_Declare(glm
+  GIT_REPOSITORY https://github.com/g-truc/glm.git
+  GIT_TAG ${RX_GLM_TAG}
+  GIT_SHALLOW TRUE)
+FetchContent_MakeAvailable(glm)
