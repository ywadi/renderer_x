# SDD ledger — plan: docs/superpowers/plans/2026-08-09-toolchain-platform-rhi.md
Task 1: minor (deferred): tools/toolchain_check has no add_test() yet (expected — brief didn't call for one; first real ctest wiring lands with Task 4's rx_core_tests)
Task 1: minor (deferred): -Wno-nullability-completeness in zig wrapper scripts flagged by reviewer as maybe Linux-irrelevant — ruling: verified correct, zig's bundled libc++ headers trigger this warning regardless of target OS (confirmed during toolchain spike before writing the plan)
Task 1: complete (commits e6afc9e..416b919, review clean)
Task 2: minor (deferred): -Wno-nullability-completeness flag in windows wrapper scripts is a Clang/ObjC-nullability flag with no Windows/MinGW relevance — plan-mandated (brief specifies verbatim), harmless (unrecognized-for-target, silently inert), same root cause as Task 1's identical note
Task 2: complete (commits 416b919..11ad1b1, review clean)
Task 3: minor (deferred): cache key doesn't hash DEP_CMAKE_ARGS, so changing a cached dep's CMAKE_ARGS without bumping TAG would silently reuse the stale build (plan-mandated, inherited from brief's verbatim DepCache.cmake code)
Task 3: minor (deferred): rx_dep_cache_key's `zig version` execute_process has no RESULT_VARIABLE check (plan-mandated, inherited); low risk since Tasks 1-2 already validate the toolchain separately
Task 3: complete (commits 337b204..5f2b375, review clean)
Task 4: CRITICAL finding (Haiku implementer's commit contained forbidden Co-Authored-By/Claude-Session trailer, violating CLAUDE.md) — FIXED directly by controller via `git commit --amend` (message-only, tree/author unchanged, verified via git log + git diff --stat showing empty diff between old/new commit trees); not pushed yet at time of fix, so no public-history exposure. Commit hash changed 0eda1db -> b508684.
Task 4: minor (deferred): report's "Deviations from Brief: None" was inaccurate (doctest_main.cpp addition was an undisclosed-in-that-section but correct and necessary deviation) — no action needed, deviation itself was sound
Task 4: minor (deferred): Handle generation is uint32_t with no overflow guard (theoretical, not reachable in practice, not required by brief)
Task 4: complete (commits 5f2b375..b508684, 1 critical fixed directly by controller + verified, 2 minors deferred)
Task 5: minor (deferred): requiredVulkanInstanceExtensions() returns {} silently on nullptr with no RX_LOG_WARN (other 2 SDL failure paths do log) — intentional per test design (dummy-driver case), low priority
Task 5: complete (commits b508684..89dd9f7, review clean)
Task 5.5 (ad-hoc, discovered during Task 5 review): windows-cross-zig fails to configure from clean once any cached dependency is present — spdlog's subbuild dies with no CMAKE_RC_COMPILER set on the Windows target. Load-bearing: blocks Task 14's CI matrix and every dependency added in Tasks 6-9. Fixing before proceeding to Task 6.
Task 5.5: fix round 1/1 (RC compiler root-caused: CMake mis-infers a "zig-" toolchain prefix from zig-cc-windows and guesses nonexistent zig-windres; fixed via new cmake/zig-wrappers/zig-rc-windows wrapping `zig rc`); windows-cross-zig now cross-builds spdlog+SDL3 cleanly from scratch; linux-native reverified clean. New concern surfaced: rx_platform's find_package(Vulkan REQUIRED) needs a Windows vulkan-1.lib that doesn't exist on this host (unrelated to RC, pre-existing since Task 5). commits 4c6ba3e..0444ab5
Task 5.5: complete (commits 4c6ba3e..0444ab5, DONE_WITH_CONCERNS — RC fix verified clean; Vulkan loader-lib gap handed to Task 5.6)
Task 5.6 (ad-hoc, discovered during Task 5.5): rx_platform links against find_package(Vulkan REQUIRED)'s loader lib it doesn't actually need (project uses volk for dynamic loading) — swap to a direct Vulkan-Headers fetch so windows-cross-zig doesn't need a Windows vulkan-1.lib. Closing this now so windows-cross-zig is clean through everything delivered so far; Task 6 (which adds vk-bootstrap) gets an explicit heads-up to check for the same pattern with its own dependency.
Task 5.5: review clean (Approved) — root cause independently reproduced and verified by reviewer from actual CMake module source; fix confirmed purely additive, zero risk to linux-native. Minor: zig-rc-windows passes bare arch instead of full triple (cosmetic, functionally identical per zig rc --help).
Task 5.6: review clean (Approved) — Vulkan-Headers tag independently re-verified against GitHub's live API (SHA matches), FetchContent-location deviation confirmed to match the plan's own established convention (Task 6's volk addition goes to the same file), no dangling Vulkan:: references anywhere. commits 0444ab5..720d988
Task 5.6: complete (commits 0444ab5..720d988, review clean)
Flagged for Task 14 (CI): rx_core_tests' log_test.cpp fails one assertion under Wine (spdlog-captured-stdout comparison, likely CRLF/line-ending artifact) — surfaced only via a bonus ctest run on windows-cross-zig, not required by any task's stated bar before Task 14. Must be resolved when Task 14 wires up the CI matrix, since CI explicitly runs ctest on both presets.
Flagged for Task 6: vk-bootstrap (added via dependency cache) may have its own find_package(Vulkan) requirement for its library target when configuring windows-cross-zig, similar to the pattern just fixed for rx_platform. Task 6 must investigate and fix if so, not assume it's fine.
Task 6: fix round 1/5 (1 addressed, 0 open — validation-layer false-positive guard isKnownPortabilityEnumerationLayerBug(), narrow two-signature match, still logged via RX_LOG_WARN; commits e908912..a5a99b6)
Task 6: complete (commits 720d988..a5a99b6, review clean after 1 fix round)
Task 6 note for later: guard's match condition (a) keys on VUID-VkInstanceCreateInfo-flags-zerobitmask alone — safe only while nothing but vk-bootstrap's portability bit ever sets VkInstanceCreateInfo::flags (documented in context.cpp comment). Revisit if instance-flags usage changes.
