# SDD ledger — plan: docs/superpowers/plans/2026-08-10-phase1-completion.md
# Continues from .superpowers/sdd/2026-08-09-toolchain-platform-rhi/progress.md (Tasks 1-6 + 5.5/5.6 complete there; that plan's Tasks 7-15 superseded by this plan)
Dispatch state (2026-08-10): Task 1 (Device, Sonnet, main tree) and Task 4 (Slang+shaders, Sonnet, isolated worktree, merged by coordinator before Task 5) both dispatched in parallel from BASE 3cdcef6. Phase 2 research agent (Sonnet, online) dispatched — findings feed coordinator-authored Phase 2 spec at .superpowers/sdd/2026-08-10-phase1-completion/phase2-research-findings.md.
Task 4: complete pending merge (commit ffa30be on branch worktree-agent-ae1008d3da87e4133, review Approved, 0 critical / 0 blocking). Merge into main deferred until Task 1's agent finishes in the main tree (no merges while an agent has the main working tree).
Task 4: deferred-with-ruling: fetch marker is existence-only, not version-keyed — bumping RX_SLANG_VERSION without clearing third_party/slang-prebuilt/ would silently keep the old binary. Ruling: correct for the current pin; MUST be fixed in Phase 2's Slang runtime-integration task, which rewrites fetch_slang.cmake anyway (libslang linking). Do not copy this fetch pattern elsewhere without fixing it.
Task 4: minor (deferred): root CMakeLists gained 4 lines (2 content + 2 blank) vs the plan's "2 added lines" — cosmetic; custom-command DEPENDS omits RX_SLANGC itself (inert while fetch is configure-time+pinned); version constants duplicated between fetch script and test script (commented cross-refs exist).
Task 1: complete (commits 3cdcef6..cb6d60d, review dispatched; commit hygiene verified clean). Implementer found+worked around a real vk-bootstrap defect (process-wide cached instance fn pointers) via doctest suite ordering — reviewer asked to assess workaround fragility, esp. for Phase 2 headless tests.
Task 4: MERGED into main (merge commit f7fc8d0, post-merge verification: full configure+build+ctest 4/4 green on linux-native, slang fetch + shader compile working in main tree).
Phase 2: spec (docs/superpowers/specs/2026-08-10-phase2-shader-resource-design.md) + plan (docs/superpowers/plans/2026-08-10-phase2-shader-resource.md) authored by coordinator from research findings; commits with this ledger entry.
Task 1: review Approved with 2 Important forward-risk findings (verified real by reviewer: reproduced SIGSEGV without --order-by=suite; confirmed vk-bootstrap caching defect against upstream source line-for-line). Entering fix round 1: (a) document the vk-bootstrap process-wide fn-pointer caching landmine in context.h production code, (b) replace filename-fragile test ordering with a structural fix (warm the vkb cache with the broadest instance at test-binary startup), (c) minor: guard recreateSwapchain against a mismatched surface.
Task 1: fix round 1/5 (3 addressed, 0 open — context.h landmine doc, structural warm-up replacing filename-fragile ordering (verified under rand seeds incl. the crash precondition + no-display fallback + warm-up-failure degradation), recreateSwapchain surface-mismatch warn; commit a3ee68e). Implementer self-caught a validation-axis regression mid-pass; re-reviewer confirmed the final warm-up covers both axes.
Task 1: complete (commits 3cdcef6..a3ee68e incl. fix round, review clean)
Task 2: complete (commits 5c641c7..fb5df51, review Approved; all three historical VMA defects verified avoided against the fetched VMA 3.4.0 source; live re-run reproduced 41/41 assertions)
Task 2: minor (deferred to final fix wave): buffer.cpp:17-19/buffer.h:4-6 comment misattributes vkGetDeviceProcAddr population to volkLoadDevice (actually volkLoadInstance per volk.c:253); buffer.h:11 stale "allocator.cpp" filename in comment.
Task 3: complete (commits 7d463e7..49ec5f6, review Approved; both deviations independently verified — defer_surface_initialization drop is the CORRECT headless selection per pinned vk-bootstrap source, coherence guard is logically airtight per VMA 3.4.0 source)
Task 3: TRACKED for Phase 2 Task 4 (now written into the Phase 2 plan): Allocator/Buffer expose no flush/invalidate surface (VmaAllocation private) — Buffer::flush/invalidate must be added when the Uploader lands; Phase 1's readback tests guard on device-wide host-coherence as a proxy.
Task 3: minor (deferred): runOnce returns void and swallows submit-path failures (logged only; caught downstream by pixel assertions) — a bool/VkResult return would be more explicit.
Task 5: complete (commits 57739df..53abcc3, review Approved; spec-violation fix verified — no swapchain image ever touched; probe coordinates hand-verified against NDC math; shaderDrawParameters requirement confirmed via spirv-dis on the real .spv; second validation guard proven a stale-layer artifact by running against a newer VVL build found on this machine)
Task 5: minor (deferred): kPixelBytes assumes 4-byte swapchain formats without a runtime assert; host-coherence check loop duplicated verbatim from clear_color_test.cpp — factor into a shared helper if a third copy appears in Task 6.
Task 6: complete (commit e6721ee, full report at task-6-report.md). FrameSync (per-frame-in-flight fences/imageAvailable semaphores/command pools, per-swapchain-image renderFinished semaphores, created once/destroyed once at device-idle shutdown) + samples/01_triangle --present mode running the real acquire/submit/present loop against actual swapchain images. Real defect found and fixed (not hypothetical): the brief's literal "wait+reset current frame's fence, then acquire" ordering deadlocks whenever acquire itself reports NeedsRecreate, since that iteration never submits and nothing re-signals an already-reset fence -- reproduced immediately via the FrameSync test's hidden window. Fixed by resetting the fence only after acquire confirms success (standard vulkan-tutorial.com-style ordering), documented at length in frame_sync.h and main.cpp.
Task 6: verified live on this machine (NVIDIA RTX 2080, driver 580.82.07): --present renders a correctly-shaped triangle (screenshot-confirmed), survives 20 scripted resizes (5 sequential + 15 randomized) with zero validation errors beyond the two documented false positives, closes cleanly via SIGTERM->SDL_EVENT_QUIT. rx_rhi_vk_tests 6/6, full ctest 5/5 green on linux-native; windows-cross-zig configures+builds clean.
Task 6: minor (deferred): MANUAL_VERIFICATION.md's Windows/Steam Deck checklists are an honest unchecked template, not fabricated results -- no access to that hardware from this environment; windows-cross-zig itself is confirmed clean. Must be filled in with a real run before any release claiming Windows/Steam Deck support (Task 8 or whenever the maintainer has hands on that hardware).
Task 6: minor (deferred, informational only): xdotool windowclose (raw XDestroyWindow, bypassing the WM_DELETE_WINDOW protocol a real close-button uses) surfaced a clean, correctly-handled -- not crashing -- failure path (recreateSwapchain fails on a destroyed platform window, loop exits via a logged return 1) during manual testing; not a defect, not reachable through normal user interaction, no action needed.
Task 6: complete (commits 3fe1fea..851b1aa, review Approved; fence wait/reset reorder verified provably correct — the plan's own literal wording had the deadlock, implementer's fix traced end-to-end; per-image renderFinished indexing + resize path structurally sound; FrameSync test 6/6, 93/93 live)
Task 6: minor (deferred to final fix wave): samples/01_triangle main.cpp leaks partially-created VkImageViews on a narrow createSwapchainViews startup-failure path (add destroySwapchainViews before that return 1); samples/01_triangle/CMakeLists.txt:14-15 stale "only mode this task builds" comment; redundant double vkDeviceWaitIdle on recreate (harmless); FrameSync early-return paths rely implicitly on trivially-idle device.
