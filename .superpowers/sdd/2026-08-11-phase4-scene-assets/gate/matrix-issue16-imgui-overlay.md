# Completeness matrix — issue #16: Dear ImGui debug overlay

## Header

- **Ticket:** #16, "Dear ImGui debug overlay (optional module)" (labels
  `phase-4`, `stage-2`). Body: "rx_debug_ui: Dear ImGui v1.92.x (core+sdl3+vulkan
  backends only), UseDynamicRendering, own descriptor pool per
  frames-in-flight, font upload via existing Uploader, rendered as a
  declared graph pass (spec D20). Hard boundary: core libs and public ABI
  stay ImGui-free."
- **Plan task:** Task 21, Stage 2
  (`docs/superpowers/plans/2026-08-11-phase4-scene-assets.md:413-417`),
  sequenced parallel with Task 20 (input) after Task 18 (scene proxies),
  both before Task 22 (shadow bridge).
- **Binding spec decision:** D20 — "Debug overlay: Dear ImGui v1.92.x as
  `rx_debug_ui`"
  (`docs/superpowers/specs/2026-08-11-phase4-scene-assets-design.md:307-313`):
  upstream SDL3 + Vulkan backends with `UseDynamicRendering`; rendered as a
  normal declared graph pass writing the backbuffer; own descriptor pool
  per frames-in-flight; font upload through the existing Uploader; core
  libraries and public ABI stay ImGui-free; docking excluded. D24-D27
  checked per the brief's binding rule — D25 applies directly (row 5
  below, and see Conflicts); D24 applies indirectly (row 5's memory-
  accounting sub-finding); D26/D27 do not apply (ImGui submits no scene
  draws — see the D24-D27 row).
- **Sources consulted (version/date):**
  - `gh issue view 16` (2026-08-18 session).
  - Plan Task 21 (413-417) and Global Constraints (13-21).
  - Spec D20 (307-313) and D24-D27 (344-440).
  - **Live-fetched, real, pinned-tag source** (not the 2026-08-11 research
    doc's paraphrase): `gh api repos/ocornut/imgui/tags` (2026-08-18) — full
    tag list confirms the actual current v1.92.x line is
    **v1.92.0 through v1.92.9, plus a v1.92.9b patch and matching
    `-docking` variants of each** — the 2026-08-11 research doc's "v1.92.6
    latest stable" claim is stale by three patch releases at gate time.
    `gh api repos/ocornut/imgui/contents/backends/imgui_impl_vulkan.h?ref=v1.92.9b`
    and `.../imgui_impl_vulkan.cpp?ref=v1.92.9b` and
    `.../imgui_impl_sdl3.h?ref=v1.92.9b` — full file contents fetched and
    read directly this session (2000-line `.cpp`, both headers in full),
    including the file's own dated, PR-numbered changelog block
    (`imgui_impl_vulkan.cpp:19-109`). This matrix cites `v1.92.9b` as the
    version its API-surface claims are pinned to; `v1.92.9` (non-`b`) is
    presumed API-identical (a `b` patch is conventionally a hotfix, not an
    API change) but was not independently diffed this session.
  - In-repo code (verified this session): `src/rx_graph/include/rx_graph/pass.h`
    (full method list of `class Pass`, read in full: `addColorOutput`,
    `setDepthStencilOutput`, `addTextureInput`, `setSideEffect`,
    `setExecute`, `setExecuteChunked`), `src/rx_graph/include/rx_graph/executor.h:174-190`
    (`class PassContext`, public `VkCommandBuffer cmd` field),
    `src/rx_graph/tests/test_execute_gpu.cpp:754-768` (established readback
    pattern: `createHostVisibleBuffer` + `vkCmdCopyImageToBuffer` +
    `invalidate()` + `mappedData()`), `third_party/CMakeLists.txt`
    (confirmed: no imgui vendoring entry exists yet — this ticket adds the
    first one; confirmed volk is this project's Vulkan loader, relevant to
    the backend's own `IMGUI_IMPL_VULKAN_USE_VOLK` convenience define).
  - `.superpowers/sdd/2026-08-11-phase4-scene-assets/research-p4-present.md`
    section 3 (2026-08-11) — used strictly as a hypothesis to check against
    the real fetched source, not as a citation; its claims did NOT survive
    re-verification unqualified (see Conflicts).
  - `.superpowers/sdd/2026-08-11-phase4-scene-assets/feature-gap-audit.md`
    — read in full; no FG item covers ImGui/debug-UI specifically (D20 was
    already a confirmed seed decision, not a gap).

---

## The matrix

| # | Feature | First-tier precedent (named, cited) | Phase-4 disposition | Library support (verified, cited) | Proposed acceptance criterion |
|---|---|---|---|---|---|
| 1 | Pinned version accuracy | N/A — version-currency check, not an engine-precedent question. | consume-now (pin corrected) | Verified via `gh api repos/ocornut/imgui/tags`: the real current v1.92.x line runs v1.92.0 → v1.92.9 → **v1.92.9b** (each with a `-docking` variant). Ticket/plan both say "v1.92.x" generically (no exact patch named), so there is no hard conflict — but the only artifact that DID name a specific patch, `research-p4-present.md` ("v1.92.6"), is now three patches stale. | Task 21 vendors `v1.92.9b` (the non-docking tag, deliberately — see row 8) exactly, records the tag and MIT license in the vendoring commit per Global Constraints' "pin exact tags, record license + tag" rule; this matrix's own API-surface citations below are valid only at this exact tag and must be re-checked if a later coordinator decision pins a different patch. |
| 2 | Backend pair: init call shape | Every first-tier ImGui integration (this is the library's own canonical pattern, not an external precedent) pairs one platform backend `Init` call with one renderer backend `Init` call. | consume-now | Verified from the real fetched `imgui_impl_sdl3.h`: `ImGui_ImplSDL3_InitForVulkan(SDL_Window* window)` is the exact platform-init entry point for an SDL3+Vulkan combo (distinct overloads exist for OpenGL/D3D/Metal/SDLRenderer/SDLGPU/Other — Vulkan gets its own named function, not a generic `InitForOther`). Renderer side: `ImGui_ImplVulkan_Init(ImGui_ImplVulkan_InitInfo* info)` (returns `bool`), paired with `ImGui_ImplVulkan_Shutdown()`. | `Overlay::create(Device&, Window&, format)` calls `ImGui_ImplSDL3_InitForVulkan(window.sdlWindow())` then `ImGui_ImplVulkan_Init(&initInfo)` in that order (platform backend before renderer backend, matching every ImGui example's own ordering); both return `bool` and must be checked (device-free-adjacent test: a deliberately-invalid `InitInfo` — e.g. `MinImageCount` below 2 — is asserted to fail loudly, not silently proceed). |
| 3 | Dynamic-rendering init surface: `UseDynamicRendering` + `PipelineRenderingCreateInfo` — the version-specific API-surface risk the dispatch brief flagged explicitly | N/A — this is a pure library-API-currency question. | consume-now (with a corrected shape — see Conflicts) | **Confirmed real, dated, breaking API change** the 2026-08-11 research doc's paraphrase did not anticipate: `imgui_impl_vulkan.cpp:45` changelog, dated **2025-09-26**: "*BREAKING CHANGE*: moved some fields in `ImGui_ImplVulkan_InitInfo`: `init_info.RenderPass` --> `init_info.PipelineInfoMain.RenderPass`, `init_info.Subpass` --> `init_info.PipelineInfoMain.Subpass`, `init_info.MSAASamples` --> `init_info.PipelineInfoMain.MSAASamples`, `init_info.PipelineRenderingCreateInfo` --> `init_info.PipelineInfoMain.PipelineRenderingCreateInfo`." The struct actually in the header (`imgui_impl_vulkan.h:104-139`): `ImGui_ImplVulkan_InitInfo` has a **nested** `ImGui_ImplVulkan_PipelineInfo PipelineInfoMain` member (`:119`); that nested struct (`:85-94`) holds `VkRenderPass RenderPass`, `uint32_t Subpass`, `VkSampleCountFlagBits MSAASamples`, and — guarded by `#ifdef IMGUI_IMPL_VULKAN_HAS_DYNAMIC_RENDERING` (itself defined only when `VK_VERSION_1_3` or `VK_KHR_dynamic_rendering` is visible before this header is included, `:76-78`) — `VkPipelineRenderingCreateInfoKHR PipelineRenderingCreateInfo`. The top-level `bool UseDynamicRendering` flag (`:127`) still exists as the research doc claimed, but the older flat `ColorAttachmentFormat`/`PipelineRenderingCreateInfo`-at-top-level shape the doc implied does **not** exist at this pinned tag — the old flat fields are present only as commented-out migration breadcrumbs (`:120-123`). | `Overlay::create()` includes `<vulkan/vulkan.h>` (or volk, already this project's loader) before `#include "imgui_impl_vulkan.h"` so `IMGUI_IMPL_VULKAN_HAS_DYNAMIC_RENDERING` is actually defined (RendererX targets Vulkan 1.3 baseline per CLAUDE.md, so this is automatic, not a special case to engineer), sets `initInfo.UseDynamicRendering = true`, and fills `initInfo.PipelineInfoMain.PipelineRenderingCreateInfo` (`sType = VK_STRUCTURE_TYPE_PIPELINE_RENDERING_CREATE_INFO_KHR`, `colorAttachmentCount = 1`, `pColorAttachmentFormats = &swapchainFormat`) — NOT a flat top-level field. A compile-time check (the code simply won't compile against the real header if written against the stale flat-field shape) is itself the acceptance gate here; no separate test needed beyond normal build success. |
| 4 | Descriptor-pool sizing guidance — a SECOND, even more recent breaking change on top of row 3 | N/A — pure library-API-currency question. | consume-now (with corrected descriptor TYPES, not just quantities) | **Confirmed real, dated, breaking change**, MORE recent than row 3's: `imgui_impl_vulkan.cpp:31-39` changelog, dated **2026-04-22**: "*BREAKING CHANGE* redesigned to use separate ImageView + Sampler instead of Combined Image Sampler... When creating your own descriptor pool (instead of letting backend create its own): Before: need at least `IMGUI_IMPL_VULKAN_MINIMUM_IMAGE_SAMPLER_POOL_SIZE` descriptors of type `VK_DESCRIPTOR_TYPE_COMBINED_IMAGE_SAMPLER`. After: need at least `IMGUI_IMPL_VULKAN_MINIMUM_SAMPLED_IMAGE_POOL_SIZE` descriptors of type `VK_DESCRIPTOR_TYPE_SAMPLED_IMAGE` + `IMGUI_IMPL_VULKAN_MINIMUM_SAMPLER_POOL_SIZE` descriptors of type `VK_DESCRIPTOR_TYPE_SAMPLER`." Confirmed in the header (`imgui_impl_vulkan.h:80-82`): `IMGUI_IMPL_VULKAN_MINIMUM_SAMPLED_IMAGE_POOL_SIZE (8)` ("Minimum per atlas") and `IMGUI_IMPL_VULKAN_MINIMUM_SAMPLER_POOL_SIZE (2)` ("Minimum for linear + nearest") — these are the real, current, exact minimums, materially more precise than the 2026-08-11 research doc's vague "~1000 descriptors of various types suffices." Doc comment on the pool itself (`:98-101`): "A `VkDescriptorPool` should be created with `VK_DESCRIPTOR_POOL_CREATE_FREE_DESCRIPTOR_SET_BIT`... As a convenience, by setting `DescriptorPoolSize > 0` the backend will create one for you." | Task 21's "own descriptor pool per frames-in-flight" (per D20/plan text) means supplying `InitInfo.DescriptorPool` directly (a real `VkDescriptorPool` handle), not the `DescriptorPoolSize`-convenience path — sized for `VK_DESCRIPTOR_TYPE_SAMPLED_IMAGE` (≥8, scaled up modestly for headroom beyond the bare minimum since the HUD will register additional textures — e.g. icon atlases — beyond just the font) and `VK_DESCRIPTOR_TYPE_SAMPLER` (≥2) as two DISTINCT pool-size entries, created with `VK_DESCRIPTOR_POOL_CREATE_FREE_DESCRIPTOR_SET_BIT` set. GPU test: assert pool creation succeeds with exactly these two type/count pairs and zero validation errors when the demo window (row 9) exercises the font atlas plus at least one additional registered texture. |
| 5 | Font-atlas upload path via the existing Uploader (interaction with #28/D25) — the dispatch brief's flagged interaction row | bgfx/Filament both route ALL runtime texture uploads through one engine-owned upload path with no vendor-UI exception; this row identifies a place ImGui's own backend does NOT fit that mold. | **log-don't-drop** (the mismatch must be documented, not silently accepted as "fine") | **Major, load-bearing finding, confirmed by reading the real `.cpp` implementation, not just the header:** font/texture upload in this pinned version does **not** go through `ImGui_ImplVulkan_Init()` at all (the 2026-08-11 research doc's "Font upload: Occurs during `ImGui_ImplVulkan_Init()`" claim is for an OLDER architecture and does not survive re-verification). Per the file's own changelog (`imgui_impl_vulkan.cpp:54`, dated **2025-06-11**): "Added support for `ImGuiBackendFlags_RendererHasTextures`, for dynamic font atlas. Removed `ImGui_ImplVulkan_CreateFontsTexture()` and `ImGui_ImplVulkan_DestroyFontsTexture()`." Confirmed in the actual `RenderDrawData()` implementation (`:553-566`): it iterates `draw_data->Textures` and calls `ImGui_ImplVulkan_UpdateTexture(tex)` for any texture needing creation/update — this INCLUDES the font atlas, now treated as just one entry in a generic texture list, uploaded lazily on first use inside the render call, not eagerly at Init time. Read `ImGui_ImplVulkan_UpdateTexture()`'s full body (`:727-933`): it does its OWN raw `vkCreateImage`/`vkAllocateMemory`/`vkCreateBuffer` calls (bypassing VMA entirely — this project's memory-budget accounting per D24 tracks VMA allocations by category, and these calls are structurally invisible to that accounting), builds its own staging buffer, records into its own dedicated `bd->TexCommandPool`/`TexCommandBuffer`, and — the library's own source comment, quoted verbatim at `:923`: `err = vkQueueWaitIdle(v->Queue); // FIXME-OPT: Suboptimal!` — **blocks the entire graphics queue** until the upload completes. This is precisely the class of blocking pattern D25 exists to eliminate from THIS project's own code (`Uploader::flush()`'s old unconditional `vkWaitForFences(...UINT64_MAX)`), reintroduced by an unmodifiable vendored dependency. | Two acceptance criteria, matching the two distinct problems: (a) **D25/Uploader non-interaction is correct, not a gap to close** — `rx_debug_ui` does NOT need to (and structurally cannot, without patching vendored ImGui, which Global Constraints' "prefer unmodified vendored deps" posture and the header's own "You can use unmodified imgui_impl_* files" comment both argue against) route font/texture upload through `Uploader`/`UploadTicket` — document this explicitly in `overlay.h` as a stated exception to D25's "all Stage-1 call sites consume tickets" list, so a future reader does not mistake the omission for an oversight. (b) **The blocking-stall exposure must be bounded and observed, not ignored**: a GPU test asserts `ImGui_ImplVulkan_UpdateTexture`'s internal `vkQueueWaitIdle` fires AT MOST ONCE during a fixed N-frame headless run of the forced-demo-window smoke test (row 9) — i.e., the font atlas (and any HUD icon textures) are created once at/near startup and never thereafter in steady state; if a HUD design later causes per-frame texture churn (e.g. dynamically-generated icon atlases), that would reintroduce a per-frame full-queue stall and must be caught by this same assertion, not discovered later as a performance regression. (c) **Memory accounting gap, log-don't-drop**: `rx_debug_ui`'s own raw Vulkan allocations are invisible to D24's per-category VMA accounting/`vmaGetHeapBudgets` report — document this as a known, small, bounded exception (font atlas + a handful of HUD textures, not scene-scale data) in the D24 memory-report's own documentation, rather than letting a reader assume the report is exhaustive. |
| 6 | Event feed: `ImGui_ImplSDL3_ProcessEvent` placement + `WantCaptureMouse`/`WantCaptureKeyboard` arbitration vs. rx_platform's own input layer (#14) | Every ImGui+SDL integration example processes every SDL event through `ImGui_ImplSDL3_ProcessEvent()` before (or alongside) the app's own event handling, then gates game-input consumption on `io.WantCaptureMouse`/`WantCaptureKeyboard`. | consume-now | Verified: `ImGui_ImplSDL3_ProcessEvent(const SDL_Event* event)` (real signature, `imgui_impl_sdl3.h`) returns `bool` (whether ImGui consumed/wants the event — not itself documented in the header's terse listing, but this is the well-established, stable ImGui convention across all platform backends). Today `Window::pumpEvents()` (`src/rx_platform/src/window.cpp:52-58`) is an empty-body `SDL_PollEvent` loop — no event is forwarded anywhere yet, so there is currently no ordering conflict to resolve, but Task 20 (#14) and Task 21 (#16) landing in parallel (per the plan's own Stage 2 sequencing, `...plan.md:462`: "{T20,T21 parallel}") means BOTH tasks need to grow the same `pumpEvents()` loop body without one silently overwriting the other's hook. | `pumpEvents()` (or its Task-20-grown successor) feeds every polled `SDL_Event` to `ImGui_ImplSDL3_ProcessEvent()` first (so ImGui's own internal state, e.g. `WantCaptureMouse`, is always current before either subsystem reads it), THEN to rx_platform's own input-state update; the mouse/gamepad-delta accumulation Task 20 builds MUST gate on `!ImGui::GetIO().WantCaptureMouse` (and keyboard movement keys on `!WantCaptureKeyboard`) before applying the result to camera/gameplay state — concretely, sample 09's fly-through camera should stop responding to mouse-look while the cursor is over an open HUD panel. Device-free test: synthesize a mouse-button-down event over a fixed known HUD widget position via the demo-window smoke test (row 9) and assert `WantCaptureMouse` is true immediately after; a headless test cannot exercise the "camera stops moving" half of this rule directly, so that half is a MANUAL_VERIFICATION row (routed to #15/sample 09, not owned here). |
| 7 | Gamepad ownership conflict — a genuine, previously-unflagged interaction with #14 | N/A — internal cross-ticket interaction, not an external precedent. | **log-don't-drop**, resolved by explicit configuration (see criterion) | **New, load-bearing finding**: `imgui_impl_sdl3.h` declares `enum ImGui_ImplSDL3_GamepadMode { ImGui_ImplSDL3_GamepadMode_AutoFirst, ImGui_ImplSDL3_GamepadMode_AutoAll, ImGui_ImplSDL3_GamepadMode_Manual }` with `ImGui_ImplSDL3_SetGamepadMode(...)`, and the header's own comment states: "Gamepad selection automatically starts in **AutoFirst mode**, picking first available `SDL_Gamepad`. You may override this. When using manual mode, caller is responsible for opening/closing gamepad." **AutoFirst is the DEFAULT** — meaning, unless explicitly overridden, ImGui's SDL3 backend will itself call SDL gamepad-open functions on whatever pad it finds, independently of and potentially racing with #14/Task 20's own `SDL_OpenGamepad`/hot-plug lifecycle management (matrix `gate/matrix-issue14-input.md`, gamepad hot-plug row) — two independent subsystems both trying to own the same physical device's open/close lifecycle is exactly the kind of bug that surfaces late, intermittently, and only under real hardware (a Steam Deck's single built-in pad, not a CI-headless environment). | `Overlay::create()` explicitly calls `ImGui_ImplSDL3_SetGamepadMode(ImGui_ImplSDL3_GamepadMode_Manual, nullptr, 0)` (zero manually-provided gamepads) immediately after `ImGui_ImplSDL3_InitForVulkan()`, so ImGui's own backend NEVER opens/polls a gamepad itself — all physical-device ownership stays exclusively with #14/Task 20's surface, and HUD gamepad-navigation (`ImGuiConfigFlags_NavEnableGamepad`) is explicitly NOT enabled this phase as a consequence (consistent with the ticket's HUD being mouse/keyboard-driven toggles, not itself a gamepad-navigable menu). Flag this as a cross-reference note for #14's own matrix rather than a row #14 needs to re-derive. |
| 8 | Frame lifecycle ordering | Every ImGui backend pair follows the identical `NewFrame → build UI → Render → backend-specific draw-data consumption` shape; this is the library's own universal contract, not an external precedent to cite. | consume-now | Verified exact function names from the real fetched headers: `ImGui_ImplVulkan_NewFrame()` and `ImGui_ImplSDL3_NewFrame()` (both `void`, no return to check) must both run before `ImGui::NewFrame()` (core, not backend-specific) each frame; after UI-building calls, `ImGui::Render()` (core) produces `ImDrawData*` via `ImGui::GetDrawData()`; `ImGui_ImplVulkan_RenderDrawData(ImDrawData* draw_data, VkCommandBuffer command_buffer, VkPipeline pipeline = VK_NULL_HANDLE)` (`imgui_impl_vulkan.h:145`) consumes it, recording directly into the caller-supplied command buffer (the `pipeline` parameter defaults to the one built at Init time — no need to pass one explicitly for the plain case). | `Overlay::beginFrame()` calls `ImGui_ImplVulkan_NewFrame()` + `ImGui_ImplSDL3_NewFrame()` + `ImGui::NewFrame()` in that order (both backend `NewFrame`s before core `NewFrame`, per every ImGui example's own convention); the graph pass's `setExecute` callback (row 9) calls `ImGui::Render()` then `ImGui_ImplVulkan_RenderDrawData(ImGui::GetDrawData(), ctx.cmd)` — no separate `ImGui::EndFrame()` call is needed (`Render()` calls it internally, per ImGui's own documented contract, not independently re-verified this session but extremely well-established public knowledge of the library). |
| 9 | Overlay-as-graph-pass: side-effect pass reading nothing, target=backbuffer | N/A — verified directly against this project's own `rx_graph` API, not an external precedent. | consume-now | **Verified against the real, in-tree `Pass` API** (`src/rx_graph/include/rx_graph/pass.h`, full method list read this session): the plan's pseudocode `addPass(RenderGraph&, targetName)` does not literally match the real API — the real declaration chain is `RenderGraph::addPass(std::string_view name, QueueClass queue = QueueClass::Graphics)` (`render_graph.h:128`) returning a `Pass&`, which then chains `.addColorOutput(targetName, desc)` (`pass.h:55`, writes the named backbuffer/HUD-target attachment) and `.setSideEffect()` (`pass.h:149-153`, "Exempts this pass from culling... e.g.... a debug overlay with no downstream consumer" — the header's OWN doc comment names exactly this use case) — no `addTextureInput()`/`addStorageBufferInput()` call at all, correctly modeling "reads nothing" (ImGui reads its own internal draw-data state, not a graph-tracked resource). The execute callback is `.setExecute(std::function<void(PassContext&)>)` (`pass.h:170`), NOT `.setExecuteChunked()` — correct, since ImGui rendering must be single-threaded/main-thread (row 8's `ImGui::Render()`+`RenderDrawData()` sequence is not chunk-safe, and D5's main-thread-only-for-similar-systems precedent applies by the same reasoning as D27's `MaterialSystem` guard, even though D27 itself does not literally bind this ticket). `PassContext::cmd` (`executor.h:176`, public `VkCommandBuffer`) is the command buffer `setExecute`'s callback records into, already inside the pass's declared color-attachment dynamic-rendering scope by the time the callback runs (per pass.h's own class-level documentation of how graphics passes are invoked). | `Overlay::addPass(RenderGraph& graph, std::string_view targetName)` implements exactly: `graph.addPass("imgui_overlay").addColorOutput(targetName, /* LOAD_OP_LOAD, not CLEAR, since backbuffer content must survive under the HUD */ desc).setSideEffect().setExecute([this](PassContext& ctx) { ImGui::Render(); ImGui_ImplVulkan_RenderDrawData(ImGui::GetDrawData(), ctx.cmd); });` — flagged as a Conflict below since the plan's literal pseudocode signature doesn't match; the coordinator should update Task 21's interface sketch to the real API shape before dispatch. The `LOAD_OP_LOAD`-not-`CLEAR` requirement is itself a new, concrete acceptance criterion this row surfaces: the plan text does not specify attachment load-op, and getting it wrong (CLEAR) would blank the rendered scene under the HUD — GPU test: render a known non-black backbuffer pattern, add the overlay pass with a forced demo window, and assert the underlying pattern is still visible outside the HUD's own drawn pixels. |
| 10 | Multi-viewport/docking explicitly excluded | D20 itself: "Docking excluded (tooling phase)." | N/A-Phase-4 | Verified, and structurally reinforced beyond a mere config-flag discipline: docking/multi-viewport in current Dear ImGui ships as an **entirely separate git ref** — `gh api repos/ocornut/imgui/tags` confirms every v1.92.x patch has a matching `vX.Y.Z-docking` tag distinct from the plain release tag. Pinning to the plain `v1.92.9b` tag (row 1), not `v1.92.9b-docking`, excludes `ImGuiConfigFlags_DockingEnable`/`ImGuiConfigFlags_ViewportsEnable` and the entire multi-viewport platform-window-creation machinery **at the source level**, not merely by convention of never setting those flags on a build that technically has them available — a stronger, retrofit-cheaper boundary than a runtime flag discipline alone. Retrofit-economics check for N/A: switching to the `-docking` tag later is a pure vendoring-pin change (re-vendor a different tag), not a redesign of anything Task 21 builds — deferring costs nothing. | No acceptance criterion needed beyond the vendoring-pin choice itself (row 1); this row exists to record that the gate checked the boundary is real and structurally enforced, not merely a documented intention. |
| 11 | Headless/CI smoke-test methodology: forced demo window readback | N/A — internal test-methodology question, resolved against this project's own established readback pattern. | consume-now | Verified established in-repo precedent: `src/rx_graph/tests/test_execute_gpu.cpp:754-768` — the pattern is `allocator.createHostVisibleBuffer(pixelBytes, VK_BUFFER_USAGE_TRANSFER_DST_BIT)` → `vkCmdCopyImageToBuffer(cmd, image, VK_IMAGE_LAYOUT_TRANSFER_SRC_OPTIMAL, readback->handle(), ...)` → `readback->invalidate()` → `std::memcpy` from `readback->mappedData()` — this is the SAME readback machinery every other GPU test in this repo already uses (samples 01/03/05/07 and multiple `rx_rhi_vk`/`rx_graph` test files, confirmed via a repo-wide grep this session), not a new mechanism this ticket needs to invent. | The GPU smoke test forces `ImGui::ShowDemoWindow()` (or a minimal known-content window, e.g. one visible colored button at a fixed position — the demo window's own layout is stable enough version-to-version to probe, but a purpose-built minimal window is more robust against a future ImGui version reflowing the demo window's contents) between `beginFrame()` and the pass's `setExecute`, feeds zero/synthetic SDL events (no live input device needed), runs the pass, and reads back a fixed pixel region known to fall inside the window's title bar or a button — asserting non-background color present (proving the overlay pass actually rendered something, not a blank/culled pass) and zero validation errors (sync validation active, matching every other GPU test's own gate). |
| 12 | Core-libs-stay-ImGui-free boundary enforcement | N/A — internal build-graph question. | consume-now (as a genuinely new mechanism — see New gaps) | **Verified absent today**: no analogous link-graph/dependency-boundary CI check exists anywhere in this repo currently (checked `.github/workflows/ci.yml` and `CMakeLists.txt` structure this session for any existing "assert target X does not link target Y" pattern — none found; the project's boundary discipline today is convention + code review, not an automated gate). | Propose a CMake-time assertion (not a separate CI step): after all targets are defined, a small CMake function walks `INTERFACE_LINK_LIBRARIES`/`LINK_LIBRARIES` transitively for each of `rx_core`, `rx_rhi_vk`, `rx_graph`, `rx_material`, `rx_platform`, `rx_task`, `rx_shader` (and `rx_scene`/`rx_asset` once they land) and fails the configure step with a named error if `imgui`/`ImGui_ImplVulkan`/`ImGui_ImplSDL3` (whatever the vendored target names end up being) appears anywhere in that closure — configure-time, not build-time or runtime, so a violation is caught before any compilation even starts. This is flagged as a genuinely new mechanism (see New gaps) since nothing like it exists in this codebase yet, and the boundary is explicitly named as "hard" in both the ticket and D20. |
| 13 | D5 thread-affinity documentation | Global Constraints: "Every new public header states its thread affinity in one line." | consume-now | Verified: Dear ImGui itself is single-threaded by design (a single global `ImGuiContext`, no internal synchronization) — extremely well-established public knowledge of the library, not independently re-verified via a fresh fetch this session (see Verification health), but consistent with every function signature seen in both fetched headers (none take a context/thread-affinity parameter, none are documented `\threadsafety`-style the way SDL3's headers are). | `overlay.h` states, in one line per Global Constraints' convention: "main-thread-only — `Overlay` wraps a single global `ImGuiContext`; `beginFrame()`/the graph pass's execute callback/any HUD-building call must run on the thread that called `Overlay::create()`." This is a documentation-only acceptance criterion (no test enforces prose), consistent with how the brief describes this convention being applied elsewhere (`bindless.h`/`upload.h`/`material_system.h` doc-only edits, Task 2). |
| 14 | D24/D25/D26/D27 touchpoints (binding-rule row) | N/A — evaluated per the research brief's binding rule. | See per-D disposition below | N/A | **D25 (upload tickets):** applies directly — fully covered by row 5 (log-don't-drop: documented exception to the ticket-consumption list, with a bounded-stall test). **D24 (memory budget/eviction):** applies indirectly — row 5's memory-accounting sub-finding (ImGui's raw `vkAllocateMemory` calls are invisible to VMA-based per-category accounting) is the concrete touchpoint; disposition log-don't-drop, documented in the memory report's own scope notes. **D26 (GPU-driven readiness: per-draw addressing/instancing/BDA):** N/A-Phase-4 — ImGui's own Vulkan backend submits its own hand-built vertex/index/draw-command stream entirely internally (visible in the fetched `.cpp`'s `RenderDrawData` implementation, not reproduced in full here for space); it is not a scene-path draw and does not go through `DrawListBuilder`/`recordDrawList` at all, so none of D26's per-draw-addressing/instancing-collapse/BDA invariants apply — retrofit-economics check: nothing about D26 landing later would require reopening `rx_debug_ui`'s design, since the two systems share no code path. **D27 (main-thread pre-resolution):** N/A-Phase-4 directly (ImGui has no `MaterialSystem::getPipeline()`/`bindInstance()` call anywhere in its own rendering path — it owns and binds its own pipeline internally), though row 9's independent main-thread-only requirement for `setExecute` (vs. `setExecuteChunked`) is the analogous constraint arrived at by different reasoning; noted so the coordinator does not need to re-derive it. |

---

## Conflicts

- **Task 21's `addPass(RenderGraph&, targetName)` pseudocode does not match
  the real `rx_graph::Pass` API.** See row 9 in full: the real chain is
  `RenderGraph::addPass(name, queue)` returning `Pass&`, then
  `.addColorOutput(targetName, desc).setSideEffect().setExecute(fn)`. Not a
  correctness bug (the plan text reads as illustrative shorthand, not a
  literal signature), but the coordinator should update Task 21's interface
  sketch to the real shape, including the `LOAD_OP_LOAD`-not-`CLEAR`
  attachment-desc requirement this row surfaces (unstated in any existing
  artifact) before dispatch, since getting the load-op wrong silently
  blanks the scene under the HUD rather than failing loudly.
- **The 2026-08-11 research doc's font-upload claim ("Occurs during
  `ImGui_ImplVulkan_Init()`") does not hold at the pinned v1.92.x tag.**
  This is not a conflict between binding project artifacts (the ticket/plan
  never assert the specific mechanism, only that font upload happens "via
  existing Uploader" — itself now shown to be structurally impossible
  without patching vendored code, per row 5), but a conflict between a
  cited fact-source document and the real, current library behavior. The
  research doc should be treated as superseded on this specific point.
- **Pinned-version specificity**: neither the ticket nor the plan names an
  exact v1.92.x patch; this matrix recommends `v1.92.9b` (row 1) as the
  real current one at gate time, correcting the only artifact that DID
  name a specific (now-stale) patch.

## New gaps

- **No link-graph/dependency-boundary CI enforcement mechanism exists
  anywhere in this repository today** (row 12) — the "core libs stay
  ImGui-free" boundary, and by extension every other "layer X must not
  depend on layer Y" boundary this project has stated as policy elsewhere
  (e.g. the ImGui-free/ABI-free boundaries implied throughout the toolchain
  spec), currently relies entirely on convention and code review, with zero
  automated backstop. This is broader than ticket #16's own scope — #16
  merely surfaces it as the first ticket with an EXPLICIT "hard boundary"
  in its own text — but the underlying gap (no configure-time or CI-time
  dependency-graph assertion capability exists in this build system at
  all) is genuinely new to this gate's findings and not named in
  `feature-gap-audit.md` or the master registry. Proposed phase fit: the
  mechanism itself (row 12's CMake-walk proposal) is cheap enough to land
  in Task 21 directly (it only needs to check targets that exist NOW), but
  the coordinator may want to register it as a reusable pattern for future
  boundary claims rather than a one-off.
- **ImGui's SDL3-backend default gamepad ownership (`AutoFirst` mode)
  racing with #14/Task 20's own gamepad lifecycle** (row 7) was not named
  in either ticket's text, the plan, or `feature-gap-audit.md` — a genuine
  cross-ticket interaction this gate surfaced by reading the real SDL3
  backend header rather than assuming ImGui's input handling is
  self-contained. Resolved in-row (explicit `Manual` mode, zero gamepads),
  not escalated further; flagged here only because it was previously
  invisible to the planning universe.

## Verification health

- **Verified first-hand, high confidence, real fetched source (not
  paraphrase):** the exact tag list (`gh api repos/ocornut/imgui/tags`);
  the full contents of `imgui_impl_vulkan.h` (280 lines), `imgui_impl_sdl3.h`
  (full), and `imgui_impl_vulkan.cpp` (2000 lines, read in full including
  its complete dated/PR-numbered changelog block and the `UpdateTexture`/
  `RenderDrawData` implementations) at tag `v1.92.9b`, all fetched via
  `gh api repos/ocornut/imgui/contents/...?ref=v1.92.9b` this session. Every
  struct field, function signature, constant value, and changelog date/PR
  number cited above was read directly from these files, not inferred or
  carried over from the 2026-08-11 research doc.
- **Verified first-hand against in-tree code:** the real `rx_graph::Pass`
  API (`pass.h`, full method list), `PassContext::cmd`
  (`executor.h:174-190`), and the established readback test pattern
  (`test_execute_gpu.cpp:754-768`).
- **Not independently re-verified this session, treated as well-established
  public knowledge of the library:** ImGui's single-threaded-by-design
  architecture (row 13) and the exact internal behavior of `ImGui::Render()`
  implicitly calling `EndFrame()` (row 8) — both extremely well-known,
  stable facts about Dear ImGui not expected to have changed, but not
  re-confirmed via a fresh doc/source read this session the way every
  Vulkan/SDL3-backend-specific claim above was.
- **UNVERIFIED / explicitly out of this session's scope:** whether `v1.92.9`
  (non-`b`) and `v1.92.9b` are API-identical (assumed, given `b`-suffix
  patch-release convention, but not diffed); the exact behavior of
  `ImGui::Render()`'s internal `EndFrame()` call and whether any edge case
  requires calling `EndFrame()` separately (not encountered in the headers
  read, but the full `imgui.cpp` core implementation was not fetched this
  session — out of scope, since the ticket only vendors core+sdl3+vulkan
  backend files, and `imgui.cpp`'s own public contract is far more stable/
  well-documented than the backend files this matrix focused its
  verification budget on).
- No dead links encountered; `gh api` calls all succeeded directly (no
  HTTP 403/404 issues, unlike some Khronos-registry fetches encountered
  while researching the sibling #25 ticket).
