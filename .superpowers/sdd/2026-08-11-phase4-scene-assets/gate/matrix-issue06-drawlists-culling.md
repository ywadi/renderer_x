# Matrix — Issue #6: Frustum and shadow-caster culling (DrawListBuilder)

**Ticket:** #6 "Frustum and shadow-caster culling" (`phase-4`, `stage-2`)
**Plan task:** Task 19 (renumbered from Task 15 by the 2026-08-18 amendment),
`docs/superpowers/plans/2026-08-11-phase4-scene-assets.md:368-406`
("DrawListBuilder — parallel culling + sort keys").
**Binding spec decisions:** D14 (draw lists with sort keys), D15 (culling:
frustum + shadow-caster, CPU, parallel), D24 (eviction invariant), D26
(GPU-driven readiness — SoA layout, instancing collapse, per-draw
addressing), D27 (main-thread pipeline pre-resolution).
**Issue amendment (binding, 2026-08-18):** the issue #6 body's own
"Amendment" section — SoA indirect-compatible layout, instancing collapse,
per-draw addressing via `firstInstance`, main-thread pre-resolution,
eviction invariant, zero-alloc caller-owned storage. Read in full below;
this amendment supersedes the original Task-15 description wherever they
conflict.

**Sources consulted:**
- `gh issue view 6 --json body,comments` (2026-08-18) — full body including
  the binding amendment section.
- Plan Task 19 interface block, `docs/superpowers/plans/2026-08-11-phase4-scene-assets.md:368-406`.
- Spec `docs/superpowers/specs/2026-08-11-phase4-scene-assets-design.md`:
  D14 (:236-245), D15 (:247-261), D24 (:344-367), D26 (:391-422), D27
  (:424-440).
- `.superpowers/sdd/2026-08-11-phase4-scene-assets/research-p4-scene.md`
  §2 (frustum culling), §3 (geometry pooling / MDI), §4 (shadow-caster
  culling), §5 (layer/mask conventions).
- `.superpowers/sdd/2026-08-11-phase4-scene-assets/claim-validation-2026-08-18.md`
  claim 3 (GPU-driven invariants), claim 7 (executor allocations — the
  zero-alloc precedent this ticket's own storage model must match).
- bgfx (bkaradzic/bgfx, GitHub, commit `1eb770679fcb451d65cc27cf5b3f5297a675b477`,
  fetched 2026-08-18): `src/bgfx_p.h` (`SortKey` struct, lines ~1211-1512),
  `src/config.h` (bit-width constants).
- Filament (google/filament, GitHub, commit `d438927c1179b9abe7ede4efab7e2d4c2858d3e1`,
  fetched 2026-08-18): `filament/src/RenderPass.h` (`CommandKey` layout,
  lines 90-289), `filament/src/RenderPass.cpp`, `filament/include/filament/Camera.h`,
  `filament/include/filament/Frustum.h`.
- Gribb, G. & Hartmann, K., "Fast Extraction of Viewing Frustum Planes from
  the World-View-Projection Matrix", http://graphics.cs.ucf.edu/cap4720/fall2008/plane_extraction.pdf.
- Akenine-Möller, Haines, Hoffman, *Real-Time Rendering* (4th ed.), the
  standard reference for the AABB-vs-plane "p-vertex/n-vertex" optimized
  test (general knowledge, not independently re-fetched this session — see
  Verification health).
- Vulkan spec, vendored header:
  `.deps-cache/Vulkan-Headers-c92e874de9fa02a7/include/vulkan/vulkan_core.h:4231-4237`
  (`VkDrawIndexedIndirectCommand`) — read directly, confirmed field-for-field.
- In-repo code: `src/rx_material/material_system.cpp:1394,1491,1549,1694-1695`
  (`RX_ASSERT_MAIN_THREAD` call sites), `src/rx_core/include/rx_core/debug_checks.h`
  (guard mechanism + test-hook seam), `src/rx_material/tests/test_material_system.cpp:854-1042`
  (existing chunked-guard test precedent).

---

## The matrix

| Feature | First-tier precedent (named, cited) | Phase-4 disposition | Library support (verified, cited) | Proposed acceptance criterion |
|---|---|---|---|---|
| Sort-key bit layout — bgfx precedent | bgfx `SortKey` encodes THREE distinct draw-key shapes from one `uint64_t` depending on `SortProgram`/`SortDepth`/`SortSequence` mode (`bgfx_p.h:1211-1420`, ASCII diagram at :1330-1390): **Draw Key 0** (sort by program) = `view\|drawBit\|type\|blend(2)\|alphaRef(1)\|program(9)\|depth(32)`; **Draw Key 1** (sort by depth) = `view\|drawBit\|type\|depth(32)\|blend(2)\|alphaRef(1)\|program(9)` — i.e. the SAME fields, reordered so depth dominates. `BGFX_CONFIG_SORT_KEY_NUM_BITS_DEPTH = 32` (full float-derived precision, `config.h:359-361`). | consume-now — directly validates D14's two-partition design (opaque = state-first key, blend = depth-first key are structurally bgfx's Draw Key 0 vs Draw Key 1, respectively). | Verified via direct source read (bit-shift constants + static_asserts + the file's own ASCII bit diagram), not inferred. | Acceptance: RendererX's own u64 key documents its bit ranges with the same rigor bgfx's source does (named constants + a comment diagram), and a unit test decodes a constructed key back into (pipeline, material, depth-bucket) fields, asserting round-trip correctness — mirroring bgfx's own `decode()` function's existence as a correctness check on itself. |
| Sort-key bit layout — Filament precedent | Filament `RenderPass::CommandKey` (`RenderPass.h:130-289`): top-down bit order `CHANNEL(3,bit61)\|PASS(2,bit58)\|CUSTOM(2,bit56)\|BLENDING(1,bit53)\|PRIORITY(3,bit50)\|Z_BUCKET(10,bit32)\|MATERIAL(32,bit0={material12\|variant8\|instance12})`; for BLENDED commands the low 47 bits are instead `~distance(32)\|blendOrder(15)\|twoPass(1)` (`RenderPass.h:100-104`). | consume-now — a second, independently-verified real-engine precedent for a two-shape (opaque vs. blended) sort key packed into one u64, corroborating D14. | Verified via direct source read + `EngineEnums.h:105` (`CONFIG_RENDERPASS_CHANNEL_COUNT = 8`). | Same as above — proposed bit budget for RendererX's opaque key: pipeline (enough bits for the expected PSO-variant count, cf. bgfx's 9-bit/512-program budget as a floor), material index, then a depth bucket (see next row) — with priority/channel/blend-order fields either reserved-but-zero or explicitly deferred (see matrix-issue05's Conflicts/New gaps, which own those fields' proxy-side existence). |
| Depth-bucket precision under reversed-Z | Two contrasting precedents, both verified: bgfx spends the **full 32 bits** on depth in its "sort by program" key (state-minimization is the primary criterion; depth ordering is a best-effort front-to-back heuristic for early-Z, not required to be exact) — `config.h:359-361`. Filament spends only **10 bits** (`Z_BUCKET_MASK`, `RenderPass.h:155-156`) — it prioritizes channel/pass/priority/material grouping over fine depth ordering within the opaque bucket. | consume-now — a bit-budget decision D14 leaves unstated ("depth-bucket" is named but no width is given). | N/A — no library dependency; a design decision. | Whichever width is chosen, the quantization must respect reversed-Z's precision distribution: **do not** linearly rescale the raw NDC depth into N bits (reversed-Z's whole point is that float32 bit-patterns near 1.0 are already densely, usefully distributed — see D13's own citation chain). The correct technique (bgfx's own implicit approach, since it stores the near-full float depth directly) is to reinterpret/truncate the monotonic float32 bit pattern (all reversed-Z depths are non-negative, so the raw IEEE-754 bit pattern is already sort-order-preserving) rather than re-deriving a bucket via multiplication. Acceptance criterion: a unit test constructs several NDC depths clustered near the near plane (where reversed-Z concentrates precision) and asserts the chosen N-bit bucket still orders them correctly (no two visually-distinct-depth objects collapse into the same bucket at typical near-plane ranges). |
| Blend back-to-front — sort-DIRECTION correctness under reversed-Z | D14: "blend sorted strictly back-to-front by depth." Under D13's reversed-Z (near=1, far=0), "front" (nearest) is the *larger* depth value and "back" (farthest) is the *smaller* one — the OPPOSITE bit-ordering direction from opaque's front-to-back pass (which wants nearest/largest-depth-value objects sorted first, for early-Z rejection benefit). bgfx's Draw Key 1 stores plain depth with no documented inversion for blended items — the caller is responsible for supplying an already-oriented depth value (verified: no inversion logic found in `SortKey::encodeDraw`/`reset()`, `bgfx_p.h:1339-1511`). Filament's BLENDED command literally negates distance (`~distanceBits`, the tilde in its own bit diagram at `RenderPass.h:103`) to flip the ascending-key-implies-ascending-sort default into back-to-front. | consume-now — this is a **correctness requirement**, not decoration: getting the sign/direction wrong for one partition silently produces the WRONG partition's ordering (early-Z-hostile opaque order, or inside-out blending) while every unit test that only checks "is the list sorted" would still pass. | N/A — design-only; verified against two independent real implementations that this is a real, historically-hit bug class (Filament's explicit bit-negation exists specifically to solve it). | Acceptance criterion: a dedicated unit test builds a synthetic scene with 3+ objects at distinct, known depths, calls `build()`, and asserts (a) the opaque partition's depth-bucket bits are monotonically DECREASING in list order (nearest, i.e. highest reversed-Z value, first) and (b) the blend partition's are monotonically INCREASING (farthest, i.e. lowest reversed-Z value, first) — asserted on the SAME depth values in both partitions via one shared test fixture, so a sign-flip bug in either direction fails immediately rather than only showing up as a subtle rendering artifact. |
| Stable vs. unstable sort — tie-break determinism | D14: "Sorting is `std::sort` on u64 keys per partition." `std::sort` (unlike `std::stable_sort`) has **no** ordering guarantee among equal-key elements — two draws with identical (pipeline, material, depth-bucket) may land in either relative order, and that order is permitted to differ between runs/implementations/thread counts. | consume-now, with an explicit tie-break requirement the ticket text does not currently specify. | N/A — C++ standard library behavior, not a third-party claim (`std::sort`'s non-stability is specified by the C++ standard itself, not a library-support question). | Because D26.3's instancing collapse depends on **identical-key runs being adjacent** (not on their relative order — collapse merges them regardless of which specific instance came first), an unstable sort is *functionally* safe for collapse correctness. It is NOT automatically safe for the ticket's own "deterministic across thread counts" requirement (see next row) unless combined with the chunk-ordered-concatenation discipline below. Acceptance criterion: either (a) pack a final deterministic tie-break (e.g. the renderable's stable creation-order index) into the low bits of the key so `std::sort`'s output is fully determined regardless of algorithm internals, or (b) sort over an input array whose PRE-sort order is itself already deterministic (see next row) and document that ties resolve by original scene order as an accepted, tested behavior. Either is acceptable; leaving it unstated (current text) is not — a CI pixel-gate comparing frames rendered with different `--threads` values will intermittently fail on tie-break-sensitive scenes otherwise. |
| Determinism across thread counts — chunk-ordered concatenation | Task 19's own step list requires "determinism across thread counts (same lists any `--threads`)" and the amendment's own culling description says AABB batches are processed "via TaskSet." The general, standard technique for deterministic parallel culling (not tied to one library) is: partition the scene into **fixed, index-range** chunks (chunk boundaries determined by object index, never by runtime thread scheduling), cull each chunk independently into its OWN output buffer, then concatenate chunk outputs in ascending **chunk-index** order (never completion order) before sorting. | consume-now. | N/A — general parallel-programming pattern, not a specific library API; the plan's own TaskSet-based chunking (D4/rx_task) already partitions by fixed index range for the executor's chunked-recording path (`src/rx_graph/executor.cpp`'s `recordChunkedPass`, matrix-issue29's domain) — the SAME discipline should carry over here. | Acceptance criterion: run `build()` against the same scene with `--threads 1`, `--threads 4`, `--threads N` (matching sample 07/09's existing `--threads` flag) and assert byte-identical `ViewLists.commands`/`.payloads` output across all three — this is exactly the test shape the ticket's own step list already names ("determinism across thread counts (same lists any --threads)"); this row concretizes the MECHANISM that must hold for that test to pass. |
| AABB vs. frustum-plane test correctness | The standard "p-vertex" (positive-vertex) optimization: for each plane, select the AABB corner most aligned with the plane's normal sign per axis; if that single corner is on the negative side, the WHOLE box is outside — one dot product per plane instead of testing all 8 corners (Real-Time Rendering, 4th ed., the standard citation for this technique — general graphics-programming knowledge, not independently re-fetched this session, see Verification health). Corroborated in-project by the SIMD-batched variant already cited in research-p4-scene.md §2 (niagara's `drawcull.comp.glsl`, MIT). | consume-now. | UNVERIFIED (not re-fetched; well-established, textbook-level technique). | Acceptance criterion: unit test with a fixed frustum and a battery of AABBs in each of the classic relative positions (fully inside, fully outside each of the 6 planes individually, straddling one plane, straddling multiple planes) asserts the exact expected in/out result per case — this is exactly Task 19's own "known in/out AABB sets (exact counters)" step, concretized into the specific case battery a p-vertex implementation must pass. |
| Degenerate / zero-extent AABB | Not covered by any external precedent doc found this session — a pure implementation edge case. A p-vertex test degrades gracefully to a point-vs-plane test when `min == max`, IF the implementation does not special-case "empty" boxes incorrectly (e.g. accidentally treating `min > max` — an invalid, not merely zero-extent, box — the same as zero-extent). | consume-now. | N/A. | Acceptance criterion: unit test with a `min == max` (single-point) AABB placed fully inside, fully outside, and exactly coincident with a frustum plane; asserts no NaN/UB and the expected boundary behavior (on-plane treated as visible, consistent with the p-vertex test's standard `>= 0` convention). |
| Very large objects (ground planes) | Same p-vertex method handles this correctly by construction (a box is only rejected when its ENTIRE extent is outside one plane) — no special-casing required if implemented generically. Directly relevant to Task 22's own acceptance scenario ("large ground plane at grazing light"). | consume-now. | N/A. | Acceptance criterion: unit test with an AABB whose X/Z extent exceeds the frustum's far-plane footprint (a "ground slab") at multiple camera positions/orientations, asserting it is never culled — a regression here would silently vanish the ground plane, an easy and severe visual bug. |
| Objects spanning far=infinity under reversed-inf-Z | Cross-referenced from matrix-issue05: Filament keeps a *separate finite* culling projection specifically because Gribb-Hartmann extraction from an infinite-far matrix yields a degenerate far-plane row (`Camera.h:138-162,292-309`). D13/D15 currently extract planes from the SAME infinite-far `viewProj()` D13 defines, with no stated handling for the degenerate plane. | consume-now — this is the CULLING-side half of matrix-issue05's Conflict #1; not re-litigated here, only made concrete as a test requirement. | N/A. | Acceptance criterion (the test that proves the ambiguity was resolved safely, whichever way the coordinator adjudicates matrix-issue05's Conflict #1): place an object at extreme depth (e.g. 100,000 world units) still inside the frustum's side/top/bottom cone and assert it is **never** culled by the far test — proving the degenerate plane (if extracted directly from the infinite-far matrix) was implemented as a safe no-op, not an accidental always-reject or sign-flipped plane. |
| Conservative shadow-caster extrusion correctness | D15 + research-p4-scene.md §4: directional light's ortho frustum fit to camera-visible world bounds, casters extruded conservatively along the light direction so off-screen casters still cast — "the classic correctness trap." Precedent citations already in research-p4-scene.md (DigitalRune, GameDev.net "Tightly Culling Shadow Casters for Directional Lights") describe the two-stage approach: camera-visible bounds set the ortho box's cross-section, then the box is extended along `-lightDir` far enough to capture any caster whose extruded shadow volume could still reach the visible bounds. | consume-now — Task 19's own step list names "off-screen-caster-still-casts case" explicitly. | UNVERIFIED beyond the research doc's own citations (not independently re-fetched this session). | Acceptance criterion (already implied by Task 19's step list, concretized here): place a caster fully OUTSIDE the camera frustum (e.g. directly behind the camera) but positioned such that its shadow, extruded along the light direction, would fall across the camera-visible ground; assert it appears in `ShadowLists` (i.e. the caster culling test is against the LIGHT's extruded frustum, never against the camera's own frustum). A second test: a caster outside BOTH the camera frustum AND the light's extruded bounds must be absent from `ShadowLists` (proving the extrusion is conservative, not "cast everything"). |
| Do BLEND-partition (translucent) objects cast shadows? | D14: "Alpha-MASK draws go in the opaque partition (their pipeline variant handles cutoff)" — implying alpha-MASK objects cast shadows normally (opaque-like). D15's shadow-caster culling text never mentions the BLEND partition at all. Filament's precedent (`RenderableManager::Builder::castShadows(bool)`) treats shadow-casting as an orthogonal per-object flag independent of alpha mode — an object CAN be both alpha-blended and a caster, or explicitly opted out. | log-don't-drop → see New gaps — the ticket text is silent on strictly-blended casters. | N/A. | Proposed criterion once resolved: either (a) BLEND-partition objects are excluded from `ShadowLists` entirely in Phase 4 (a documented, tested limitation — cheap, matches most engines' default), or (b) they are included via the same `castShadows` bool as opaque/mask objects, rendered into the shadow map using their opaque-equivalent depth-only variant. Either is acceptable; the current silence is not (see New gaps). |
| Exact counter definitions for CI gates (D18) | D18: CI perf gates assert "deterministic counters (draws submitted, draws culled, instances, pool bytes, descriptor allocations)... exact and noise-free." Task 19 names `CullCounters` and "counters report records-in vs draws-submitted" (D26.3) but no field list or per-field semantics exists in any artifact read this session. | consume-now — this is a concretization gap, not a missing capability. | N/A. | Proposed exact field list (acceptance criterion IS this list, made concrete and testable): `totalCandidates` (renderables considered before any filter), `culledByLayerMask` (removed by camera cullMask/renderable layers AND-test, matrix-issue07's domain), `culledByFrustum` (passed layer filter, failed AABB-vs-plane test), `visible` (= totalCandidates − culledByLayerMask − culledByFrustum), `recordsIn` (draw records generated from `visible`, pre-collapse — one per submesh-instance pairing), `drawsSubmitted` (post-D26.3-collapse command count), `shadowCastersConsidered`/`shadowCastersVisible` (the `ShadowLists` equivalents). Each field's definition must be unambiguous enough that a unit test can assert an EXACT integer (per D18's "exact and noise-free" requirement), not a range. |
| D26.2 — SoA `VkDrawIndexedIndirectCommand`-compatible layout | Amendment text: "packed `VkDrawIndexedIndirectCommand`-compatible command array... not an AoS record struct." | consume-now — binding, D26.2. | **Verified exact match**: Vulkan's `VkDrawIndexedIndirectCommand` (`vulkan_core.h:4231-4237`) is `{uint32_t indexCount; uint32_t instanceCount; uint32_t firstIndex; int32_t vertexOffset; uint32_t firstInstance;}` — field-for-field identical to the plan's `DrawCommand` struct (plan:376). This is a real, checked claim, not an assumption. | Acceptance criterion: `static_assert(sizeof(DrawCommand) == sizeof(VkDrawIndexedIndirectCommand))` plus a test that reinterpret-casts a `std::vector<DrawCommand>` directly into a `VkMultiDrawIndexedIndirectCommand`-shaped upload without a copy/transform step — proving the "GPU-uploadable, not an AoS record struct" claim is actually exercised, not merely structurally coincidental. |
| D26.2 — per-block grouping (`blockId`) | Amendment text: grouped by GeometryPool `blockId`, "the recorded per-block indirect submission bound (one future MDI call per block)." | consume-now for the grouping; the `blockId`/GeometryPool source itself is Stage-1 (asset-pipeline) scope, cross-referenced only. | UNVERIFIED — GeometryPool (Task 12, Stage 1) is outside this gate agent's assigned tickets; whether it exposes a stable `blockId` per allocation was not independently checked this session. | Acceptance criterion (DrawListBuilder's own half): `ViewLists.blocks` groups `DrawCommand`s contiguously by `blockId` within each sort partition (sort key's material/pipeline ordering must not scatter same-block draws non-contiguously in a way that breaks the "one MDI call per block" invariant) — a unit test asserts, for a synthetic multi-block scene, that each block's commands occupy one contiguous `BlockRange`. |
| D26.3 — instancing collapse, collapse-key definition | Amendment text: "runs of identical (pipeline, material, mesh range, block) collapse into instanced draws." | consume-now — binding, D26.3. | N/A — RendererX-specific design, no external library involved. | See next two rows for the concrete correctness subtlety and counters. |
| D26.3 — what breaks collapse (explicit + an implicit requirement this gate found) | Explicit (from the amendment): pipeline, material, mesh-range, or block divergence each break a run. **Implicit, not stated in D26.1/D26.3/Task 19 text:** D26.1 drives per-draw materials via `firstInstance` indexing into a bindless per-draw buffer — meaning a collapsed run's instances must occupy **contiguous** slots in that per-draw payload buffer starting at the collapsed command's `firstInstance`, or `gl_InstanceIndex`-based per-instance lookup in the shader silently reads the WRONG instance's data. This means the `DrawPayload` array must be re-ordered in lockstep with `DrawCommand` during the sort/collapse pass (sorting the two arrays independently, or collapsing commands without moving payloads to match, would violate this silently — no counter would catch it, since the counts still balance). | consume-now — flagged as a correctness-critical implementation detail, not optional polish. | N/A. | Acceptance criterion: a unit test builds a scene where several identical-key renderables are interleaved in creation order with NON-identical-key ones (so a naive per-array-independent sort would desynchronize commands and payloads), runs `build()`, and asserts every collapsed `DrawCommand`'s `[firstInstance, firstInstance + instanceCount)` range in `ViewLists.payloads` contains ONLY payload entries belonging to that exact collapsed group (i.e. cross-checks payload identity against the source renderables, not just array lengths). |
| D26.3 — counters (records-in vs draws-submitted) | Amendment text: "counters report records-in vs draws-submitted." | consume-now. | N/A. | Acceptance criterion: a synthetic scene with a known count of identical-state instances (e.g. 1000 identical renderables) asserts `recordsIn == 1000` and `drawsSubmitted == 1` (single collapsed instanced draw) — the exact "identical-run scene → 1 command with instanceCount=N" case Task 19's own step list already names, made numerically concrete. |
| D26.1 — per-draw addressing via `firstInstance`, zero per-draw push constants | Amendment text: "`recordDrawList` drives materials via `firstInstance` indexing... zero per-draw push constants in the scene path." Directly named as the anti-pattern to avoid: "sample 07's push-constant loop" (D26 spec text, :399-401). | consume-now — binding, D26.1. | UNVERIFIED whether sample 07's actual push-constant loop was independently re-read this session (referenced from spec text, not re-confirmed against `samples/07_stress` source). | Acceptance criterion: a GPU test (or a static/link-time check) asserts the scene-path recording helper (`recordDrawList`) never calls a push-constant-update entry point per draw — e.g. by counting `vkCmdPushConstants` calls in a Tracy/validation-layer-observable way across an N-instance draw and asserting the count is O(1) per pass, not O(draws). |
| D27 — enumeration of distinct pipeline keys from the sorted list | D27 spec text: "nearly free, because the sorted draw list already enumerates every distinct pair before any command is recorded." | consume-now — binding, D27. | N/A — algorithmic consequence of the sort already required by D14, not a library claim. | Acceptance criterion: the pre-resolution pass is demonstrated (via a counter or Tracy zone) to be a single **linear** scan over the already-sorted list (boundary-change detection on (material, pass-signature, specialization)), not a second O(n log n) grouping pass — i.e. its cost must be shown to scale linearly with list size in the published stage-exit numbers (CLAUDE.md's measured-claims policy). |
| D27 — parameter-offset pre-computation | D27 spec text: "pre-resolves every distinct (material, pass-signature, specialization) pipeline and per-draw parameter offsets on the main thread before fan-out." | consume-now. | N/A. | Acceptance criterion: worker chunks' recording callback signature/captured state contains ONLY plain resolved data (`VkPipeline` handles, integer offsets) — a unit/API-shape test asserts the chunk callback type has no reachable path to `MaterialSystem::getPipeline`/`bindInstance` (e.g. by not capturing a `MaterialSystem&` at all in the chunked closure), which is a stronger, compile-time-adjacent guarantee than a runtime-only check. |
| D27 — worker-guard test design | Existing in-repo mechanism, already used for exactly this problem shape: `RX_ASSERT_MAIN_THREAD(context)` (`src/rx_core/include/rx_core/debug_checks.h:24-90`) guards `MaterialSystem::loadMaterial`/`bindInstance`/`reloadChanged`/`getPipeline` today (`material_system.cpp:1394,1491,1549,1694-1695`) and aborts (production) or invokes a test-installed `ViolationHook` (`rx::core::debug::detail::setViolationHookForTests`) instead of crashing, specifically so a test can observe a violation without terminating. `test_material_system.cpp:854-1042` already implements the exact test shape needed: a genuine two-chunk rendezvous barrier proving a worker thread (not just "probably" a worker — the file's own comment documents a prior false-negative, "2/30 runs where every chunk landed on the main thread") calls the guarded API, with the hook capturing the violation instead of aborting. | consume-now — **do not build new infrastructure**; reuse this mechanism verbatim (directly satisfies CLAUDE.md's "prefer ready-made... over writing subsystems from scratch," here meaning in-repo ready-made). | **Verified in-repo, exact file:line citations above** — read directly this session, not inferred from the claim-validation doc's prior citation (which named the same line numbers; independently re-confirmed, zero drift). | Acceptance criterion: DrawListBuilder's chunked recording test reuses the SAME `setViolationHookForTests` + rendezvous-barrier pattern as `test_material_system.cpp:906-1042`, asserting zero violations fire when `recordDrawList` runs across `chunkCount > 1` with genuine worker-thread participation proven by the barrier (not assumed) — this is Task 19's own named step ("pre-resolution unit test (worker chunks never hit the main-thread guard — assert under a debug hook)") with the exact existing mechanism identified. |
| D24 — residency-tolerant handle resolution at list-build time | D24(e): "residency-tolerant handle resolve (a resolve may yield the fallback while nonresident)... one working deferred-eviction path proving the system tolerates it." Amendment: "handle resolution at list-build time is residency-tolerant (fallback substitution, never a crash or raw-pointer escape)." | consume-now — binding, D24 + issue #6 amendment. | Cross-references D11 (fallback assets) — not independently re-verified this session (Stage-1/asset-pipeline scope). | Acceptance criterion: a unit test force-evicts a mesh/material a `Scene` renderable references (test hook, mirrors matrix-issue05's equivalent proxy-level test) immediately before `DrawListBuilder::build()`, asserting the build succeeds and substitutes the fallback asset for that draw's material/mesh index — never a crash, never a stale pointer/index escaping into `ViewLists`. |
| Zero-alloc caller-owned storage — capacity growth policy | Amendment: "`build()` writes into caller-owned reused storage... zero net heap allocations across steady-state frames." Directly analogous to matrix-issue29's `rx_graph::Executor` zero-alloc requirement (same phase, same discipline, verified precedent: `Impl`-persistent buffers cleared-not-reallocated per frame). | consume-now — binding. | Cross-references matrix-issue29's own findings on the SAME discipline applied to `Executor::execute()` — internally consistent precedent, not an external library. | Acceptance criterion: `ViewLists`'s vectors are `.clear()`d (retaining capacity) at the start of each `build()`, never reset via `= ViewLists{}`/`shrink_to_fit`; growth (an actual allocation) is permitted ONLY on frames where the scene's visible-object count exceeds the highest count previously seen (amortized geometric growth, standard `std::vector::push_back` behavior) — a counting-allocator or capacity-snapshot test (see matrix-issue29 for the concrete mechanism, since no such hook exists in-repo yet) asserts zero allocations across N steady-state frames after a warm-up frame establishes peak capacity. |
| Zero-alloc caller-owned storage — `ShadowLists` shape | Task 19's interface block declares `buildShadow(..., ShadowLists& out)` but never defines the `ShadowLists` type's fields — unlike `ViewLists`, which is fully specified (plan:378). | log-don't-drop → New gaps. | N/A. | Proposed shape (acceptance criterion once adopted): mirror `ViewLists`'s `commands`/`payloads`/`blocks`/`counters` fields, but with a SINGLE partition (shadow maps are depth-only in Phase 4 — D21's baseline has no blend-caster path per the row above), sorted by (pipeline, mesh, block) only (no material-index-driven color state to minimize, though alpha-MASK casters still need their cutoff-variant pipeline). This needs the coordinator's decision on the BLEND-caster question above before the shape is final. |
| Alpha-MASK draws in the opaque partition | D14: "Alpha-MASK draws go in the opaque partition (their pipeline variant handles cutoff)." | consume-now — already fully specified; included here only to confirm no conflict was found against any precedent (both bgfx and Filament treat alpha-test as an opaque-adjacent state bit, not a separate partition — consistent). | N/A. | Acceptance criterion: unit test with a mixed opaque/alpha-MASK/blend scene asserts alpha-MASK draws appear in the opaque partition's sort order (by pipeline/material/depth-bucket, front-to-back), never in the blend partition. |

---

## Conflicts

1. **Frustum-plane extraction from an infinite-far reversed-Z matrix — see
   matrix-issue05 Conflict #1.** Repeated here because it is this ticket's
   own implementation responsibility, not just an API-shape question:
   D15's text ("camera planes extracted from the (reversed-Z) view-proj")
   and D13's infinite-far commitment are not reconciled anywhere in the
   read artifacts. Quoted in full in matrix-issue05; not re-quoted here to
   avoid duplication, but flagged again since matrix-issue06 is where the
   actual culling code lives.
2. **Priority/blend-order sort-key tiers are absent from D14 but are
   load-bearing fields in both cited precedents' actual sort keys** (bgfx's
   `blend`/`alphaRef` bits; Filament's `PRIORITY`/blend-order bits are not
   decoration — they occupy dedicated, always-present bit ranges in every
   key variant). D14's key is `(pipeline, material, depth-bucket)` only.
   Quote, D14: "sort keys ... opaque front-to-back by pipeline\|material\|depth." Quote, Filament source comment implicit in the bit layout itself
   (`RenderPass.h:158-168`): priority and blending occupy bits 50-53,
   ABOVE material/z-bucket, in every draw command. Not resolved here; the
   proxy-side half of this conflict is recorded in matrix-issue05.

## New gaps

- **BLEND-partition shadow-casting is unaddressed.** D14 states alpha-MASK
  goes to the opaque partition (implying it casts normally); D15 is silent
  on strictly alpha-BLENDED objects. Proposed phase fit: resolve in Task 19
  itself (Phase 4 Stage 2) — deferring the DECISION (not necessarily the
  feature) costs nothing now, but leaving it silently unstated risks an
  untested, undefined behavior shipping in the reference scene (sample 09
  may well include blended foliage/glass that either silently doesn't cast
  a shadow or silently does, with no test proving which).
- **`ShadowLists`' concrete field layout is undefined** in every artifact
  read (plan, spec, issue). Proposed phase fit: Task 19 (this ticket),
  since `ShadowLists` is this ticket's own deliverable type and the
  ambiguity blocks writing Task 19's own named "off-screen-caster-still-casts" test precisely.
- **Exact `CullCounters` field list is undefined** beyond the two names
  "records-in"/"draws-submitted" (D26.3) and the general category list in
  D18. Proposed phase fit: Task 19 (this ticket) — D18 already requires the
  counters to be CI-gateable and exact; an underspecified struct cannot be
  gated exactly.

## Verification health

**Verified first-hand this session (fetched/read, cited by file:line, URL,
or commit SHA):** bgfx `SortKey`/`config.h` at commit
`1eb770679fcb451d65cc27cf5b3f5297a675b477`; Filament `RenderPass.h`/`.cpp`,
`Camera.h`, `Frustum.h`, `EngineEnums.h` at commit
`d438927c1179b9abe7ede4efab7e2d4c2858d3e1`; the vendored Vulkan header's
`VkDrawIndexedIndirectCommand` definition; `RX_ASSERT_MAIN_THREAD`'s
mechanism and the existing `test_material_system.cpp` chunked-guard test
(re-confirmed against current HEAD, zero line-number drift from the
claim-validation doc's own citations); Task 19/issue #6 plan and spec text.

**Inferred / not independently re-verified:** the p-vertex AABB-vs-plane
technique (textbook-standard, cited to *Real-Time Rendering* 4th ed. from
general knowledge, not re-fetched); the DigitalRune/GameDev.net
shadow-caster-culling citations (carried over from research-p4-scene.md's
own prior citation, not independently re-fetched this session); whether
GeometryPool (Stage 1, outside this agent's ticket scope) actually exposes
a stable `blockId` usable the way this matrix assumes; whether sample 07's
push-constant loop was re-read directly (relied on the spec's own D26
citation of it).

**Dead links / version ambiguity:** none encountered. bgfx and Filament were
both pinned to a commit SHA captured this session rather than cited against
a floating branch reference.
