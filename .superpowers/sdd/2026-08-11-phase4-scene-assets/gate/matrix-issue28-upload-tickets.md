# Matrix — Issue #28: Uploader completion tickets: non-blocking flush invariant

**Plan task:** Task 11 (`docs/superpowers/plans/2026-08-11-phase4-scene-assets.md:219-242`).
**Spec decision binding this ticket:** D25 (primary). Related: D5
(threading contract — the API stays main-thread-only, unchanged by this
ticket), D26.1 (per-draw addressing — unrelated to upload mechanics,
not touched by this matrix), the master registry's "Upload/transfer
asynchrony policy" deferred entry
(`docs/superpowers/specs/2026-08-09-toolchain-platform-rhi-design.md:267-274`)
— the boundary this ticket must NOT cross (cross-queue submission,
queue-family ownership transfer, multi-frame in-flight staging all stay
streaming-phase).

**Sources consulted:**
- Delivered code, `src/rx_rhi_vk/` (HEAD `bf5b853`), read in full:
  `include/rx_rhi_vk/upload.h` (214 lines), `src/upload.cpp` (368 lines),
  `include/rx_rhi_vk/mesh_buffers.h` (78 lines), `include/rx_rhi_vk/device.h`,
  `src/device.cpp` (queue-acquisition section, lines 229-333),
  `samples/04_streaming/main.cpp` (call sites at lines 907-920, 1281, 1580).
- `docs/threading.md` (D5 contract, full read).
- vk-bootstrap, pinned commit `556b79b165386f6c1a18362d30f2a076fdaa2778`
  (`third_party/CMakeLists.txt:243-249`), header
  `.deps-cache/vk-bootstrap-*/include/VkBootstrap.h` — read first-hand.
- Vulkan Documentation Project tutorial, "Synchronization and Streaming"
  (docs.vulkan.org/tutorial/latest/Building_a_Simple_Engine/Advanced_Topics,
  fetched 2026-08-18).
- Khronos Vulkan Registry man pages: `vkGetFenceStatus`,
  `VK_KHR_timeline_semaphore`, `vkGetSemaphoreCounterValue` (all
  registry.khronos.org, latest spec, read 2026-08-18).
- Microsoft Learn, "Fence-Based Resource Management" (Direct3D 12 docs,
  learn.microsoft.com, fetched 2026-08-18) — cross-API precedent for
  fence-gated ring-buffer reclamation.
- `KhronosGroup/Vulkan-ValidationLayers` issue #5997 (GitHub, fetched
  2026-08-18) — known false-positive re: concurrent `vkGetFenceStatus`.
- `claim-validation-2026-08-18.md` claim 1 (the understated claim this
  ticket originates from) and claim 6; issue #28 body; plan Task 11 text.

---

## The matrix

| # | Feature | First-tier precedent (named, cited) | Phase-4 disposition | Library support (verified, cited) | Proposed acceptance criterion |
|---|---------|--------------------------------------|----------------------|-------------------------------------|-------------------------------|
| 1 | **CRITICAL — current single-fence-reused-per-flush design is structurally incompatible with a pollable ticket, unmodified** | N/A — this is a direct code-correctness finding, not a precedent gap. | consume-now (this IS the ticket's core redesign) | Verified: `fence_` is a single `VkFence` member created once in `Uploader::create()` (upload.cpp:67-72, stored at upload.h:199); every `flush()` call does `vkResetFences(fence_)` → `vkQueueSubmit(..., fence_)` → (today) `vkWaitForFences(fence_, UINT64_MAX)` (upload.cpp:266-278) reusing the SAME fence object every time. If `flush()` is changed to return immediately with a ticket instead of waiting, a SECOND `flush()` call before the caller has polled/waited on the FIRST ticket would `vkResetFences()` the same handle the first ticket's `isComplete()`/`wait()` still needs to query — the ticket's identity becomes entangled with a mutable, reused fence object, silently corrupting the earlier ticket's completion signal (either indistinguishable from the new submission, or a validation error from resetting a fence whose signal a caller may still be relying on). The Vulkan spec requires fences be reset only when not associated with any queue command still tracked (`vkResetFences` valid-usage: `VK_ERROR_...` / undefined behavior if reset while a wait is pending elsewhere) — this precondition breaks the instant flush() stops blocking. | Whatever mechanism row 2 selects (fence pool or timeline semaphore) MUST close this hole: a test issues two overlapping `flush()` calls without waiting on the first ticket in between, then asserts `isComplete()` on the FIRST ticket still reports its own true status (not the second submission's), and that no fence-reset-while-referenced validation error occurs. This is the single highest-priority correctness gate for this ticket. |
| 2 | UploadTicket completion-tracking primitive: fence-per-flush (small rotating fence pool) vs timeline-semaphore-per-batch | **Fence-per-batch, pool-rotated**: this codebase's own existing per-frame pattern already does this for `FrameSync` (a small fixed pool of fences, one per frames-in-flight slot, each reset only once its own prior use is confirmed complete) — the natural extension of an already-adopted idiom, not a new one. **Timeline-semaphore-per-batch**: Vulkan Documentation Project's own "Synchronization and Streaming" tutorial (docs.vulkan.org, fetched 2026-08-18) demonstrates exactly this shape for a background uploader: "A dedicated thread: 1. Batches pending copies... 3. Submits once, signaling a monotonically increasing timeline value... The render submit includes a wait on the latest uploads timeline value" — no reset ever needed (the value only increases), sidestepping row 1's hazard entirely by construction. FACTS presented for both; the ticket's own proposed struct shape (`UploadTicket` = "fence + ring generation," issue #28 body) suggests option A was the original intent — flagged as a design choice for the coordinator, not resolved here. | consume-now (mechanism selection is the ticket's core decision — facts only, not resolved by this matrix) | Both are core-Vulkan-1.3 primitives, no extension gating needed either way: plain `VkFence`/`vkGetFenceStatus` is 1.0 core; `VkSemaphoreType=VK_SEMAPHORE_TYPE_TIMELINE`/`vkGetSemaphoreCounterValue` was promoted to Vulkan 1.2 core (`VK_KHR_timeline_semaphore` man page, registry.khronos.org, confirmed 2026-08-18: "All functionality in this extension is included in core Vulkan 1.2"), and this project's baseline is 1.3 — neither needs opportunistic/optional gating the way BDA or `VK_EXT_memory_budget` do. | Either design must satisfy: (a) `isComplete(ticket)` is a pure poll, no blocking, no host-synchronization requirement across threads (both primitives support this per row 3/4); (b) a ticket from flush() N remains independently queryable while flush() N+1, N+2... have since been issued, without limit on how far behind the caller is (a fence-pool design must therefore size its pool to the realistic max in-flight depth, or fall back to allocating a fresh fence when the pool is exhausted — this sizing policy must be explicit, not implicit); (c) `wait(ticket)` on an already-completed ticket returns immediately (both primitives support this trivially). |
| 3 | `vkGetFenceStatus` as the poll primitive for `isComplete(ticket)` (fence-pool design) | N/A — direct Vulkan spec row. | consume-now (if fence-pool chosen) | Verified via Khronos registry (`vkGetFenceStatus` man page, read 2026-08-18): returns `VK_SUCCESS` (signaled) or `VK_NOT_READY` (unsignaled) as its two "success" codes, or a device-loss/OOM error; requires **no host synchronization** (safe to call from multiple threads on the same fence without external locking, per spec — though D5 keeps this single-threaded/main-thread-only regardless); polling it repeatedly in a loop is an explicitly valid, spec-sanctioned usage pattern (confirmed via registry + community usage guidance). | `isComplete(ticket)` calls `vkGetFenceStatus` exactly once per call (no internal spin-loop — the caller's own frame loop provides the polling cadence) and maps `VK_SUCCESS`→true, `VK_NOT_READY`→false, any error→a loud logged failure (not silently treated as either state). |
| 4 | `vkGetSemaphoreCounterValue` as the poll primitive (timeline-semaphore design) | N/A — direct Vulkan spec row. | consume-now (if timeline-semaphore chosen) | Verified via Khronos registry (`VK_KHR_timeline_semaphore` / `vkGetSemaphoreCounterValue` man pages, read 2026-08-18): `VkResult vkGetSemaphoreCounterValue(device, semaphore, uint64_t* pValue)` — a host-side query returning the semaphore's current counter value with no wait involved; `isComplete(ticket)` becomes `currentValue >= ticket.targetValue`, a pure comparison after one query call. | Same shape as row 3's criterion, adapted: `isComplete(ticket)` queries the current counter value once and compares against the ticket's stored target value; `wait(ticket)` uses `vkWaitSemaphores` with that same target value and `UINT64_MAX` timeout (the direct analog of today's `vkWaitForFences(..., UINT64_MAX)` at upload.cpp:278). |
| 5 | Ring-buffer reclamation keyed to ticket completion — partial-reclaim granularity | Direct3D 12's documented fence-gated ring-buffer pattern (Microsoft Learn, "Fence-Based Resource Management," fetched 2026-08-18) is the load-bearing cross-API precedent: a `FrameResourceOffset` queue records, per submitted batch, the fence value that batch will complete under AND the ring offset at that point; reclamation polls `GetLastCompletedFence()` first (row 3/4's non-blocking poll), pops every queue entry whose fence value is `<=` the completed value, and *only if still insufficient* falls back to `SetEventOnFenceCompletion` + block (row 6) on the SPECIFIC next-needed batch — never a whole-ring flush-and-wait. Quoted from source: `FreeUpMemoryUntilFrame(lastCompletedFrame)` pops entries in fence-value order until it hits one not yet complete. | consume-now | N/A — this is an architecture pattern applied on top of the Vulkan primitives from rows 3/4, not a library call. **Current behavior this must replace** (verified, upload.cpp:102-126): `reserveRingSpace()` today has NO partial-reclaim concept at all — on wrap, it calls `flush()` unconditionally (a full synchronous submit+wait of everything recorded so far), which is the "whole-ring reclaim, always" degenerate case the D3D12 pattern explicitly improves on. | Redesign `reserveRingSpace()`/its ticket-aware successor to maintain a queue of (ticket, ring-offset-at-issue) entries exactly like the D3D12 pattern; on wrap, first poll+reclaim every entry whose ticket is already complete (no blocking), and only block (`wait()`) on the SPECIFIC oldest still-in-flight ticket covering the needed range if reclaiming completed entries alone isn't enough — this is precisely what plan Task 11's own text already specifies ("wrap path waits only for the oldest in-flight ticket covering the needed range"); this matrix grounds that requirement against a concrete, cited, working precedent rather than leaving it to be reinvented from scratch. A stress test (many small uploads exceeding ring size, per the plan's own steps) asserts reclamation only blocks when genuinely necessary — instrumented via a counter of "blocking waits triggered" that must stay below "flush() calls issued" once tickets are in play, proving partial reclaim actually engaged. |
| 6 | Ring exhaustion under in-flight tickets: grow vs block-on-oldest policy | D3D12 precedent (row 5) chooses **block-on-oldest** as its documented fallback (never silently grows the upload heap mid-stream in the sample shown) — but explicitly notes "the ring buffer must be large enough to cope with the worst-case scenario," i.e. sizing is an app-level responsibility, not something the mechanism papers over by growing unboundedly. | consume-now — **block-on-oldest**, matching both the D3D12 precedent AND the ticket's own explicit text ("wrap path waits only for the oldest in-flight ticket") | N/A — policy row, not a library-support question; FACTS presented per the brief's mandate (grow vs block-on-oldest), coordinator/ticket text has already effectively chosen block-on-oldest — flagged here so the matrix's job of "deepen into concrete criteria" is done, not to re-litigate the choice. | A dedicated stress test (ring sized deliberately small relative to a burst of uploads, exceeding total capacity many times over across many tickets) asserts: (a) the engine never silently grows the ring buffer's allocation (a fixed-size resource, sized at `Uploader::create()` time, per the existing `ringBufferSize` parameter); (b) uploads still complete correctly and in the right destination locations despite heavy wrap-and-block cycling (data-correctness readback, not just "didn't crash"); (c) blocking only occurs when genuinely necessary (ties to row 5's counter-based proof). |
| 7 | Image staging (always-staged path) interaction with tickets | N/A — direct consequence of this codebase's own already-documented constraint (upload.h:68-76: no `VK_EXT_host_image_copy` in the Vulkan 1.3 baseline, so every image upload goes through the ring, unconditionally, by construction of the Vulkan API itself). | consume-now | Verified: `uploadToImage()` (upload.cpp:191-247) has no direct-path branch at all — every call reserves ring space, records a `vkCmdCopyBufferToImage` (+ optional mip-chain blit), and only completes once that recorded command actually executes on the GPU. This means an image upload is ALWAYS covered by whatever ticket the containing `flush()` returns — there is no "trivially complete" case for images the way row 8 identifies for buffers. | A ticket covering a `flush()` that recorded ONLY image uploads behaves identically to one covering buffer uploads: `isComplete()` false until the GPU copy+blit completes, `wait()` blocks until then. Test: a texture-only upload batch's ticket reports incomplete immediately after `flush()` returns (proving no accidental "trivially done" shortcut leaks into the image path) and complete only after the GPU work is confirmed finished. |
| 8 | Buffer direct path (ReBAR desktop / UMA Steam Deck) — ticket semantics when NO GPU work is queued at all | N/A — this is the ticket's own required "Steam Deck/UMA row," a direct consequence of this codebase's own documented direct-path mechanism (upload.h:35-66), not an external precedent. | consume-now | Verified: when `dst.directPathCapable() && dst.mappedData() != nullptr` (upload.cpp:143), `uploadToBuffer()` does a synchronous `memcpy` + `Buffer::flush()` (a host-side `vmaFlushAllocation` cache-flush, NOT a GPU submission) and returns — `everUsedDirectPath_ = true`, **no command is recorded, no ring space reserved, no fence/semaphore work queued at all** (upload.cpp:143-159). On the Steam Deck's UMA, essentially every device-local buffer destination is expected to land `directPathCapable() == true` (DEVICE_LOCAL + HOST_VISIBLE is the common case on a unified-memory APU), so a Deck build could plausibly issue an entire `flush()` covering ONLY direct-path uploads. | **Explicit semantics required, not left implicit**: a ticket returned from a `flush()` whose recorded batch contains ONLY direct-path buffer uploads (zero ring-staged uploads, zero image uploads) must report `isComplete() == true` IMMEDIATELY — by the time `flush()` returns, the memcpy+cache-flush already fully happened synchronously on the calling thread; there is no outstanding GPU work for any fence/semaphore to represent. The ticket design must either (a) special-case an "already complete" ticket sentinel when `beginRecordingIfNeeded()` was never actually entered (no `vkBeginCommandBuffer` ever called — verified this already happens today, `recording_` stays false, per upload.cpp:85-100's early-return when nothing triggers recording), or (b) still submit a trivial/empty command buffer + fence purely to give the ticket a uniform completion signal — (a) is cheaper and consistent with `flush()`'s CURRENT no-op-when-nothing-recorded behavior (upload.cpp:249-252, `if (!recording_) return;`). Test: a Deck-representative (or ReBAR-desktop) direct-path-only upload batch's ticket reports complete on the same call stack as `flush()`, with zero fence/semaphore machinery touched. |
| 9 | Mixed direct-path + staging-path uploads within one `flush()` batch | N/A — direct consequence of rows 7/8 combined; not independently precedented. | consume-now | Verified: `uploadToBuffer()` decides direct-vs-staging PER CALL (upload.cpp:128-189), so a single batch between two `flush()` calls can freely mix both — some destinations landing direct-path (immediate, no command recorded), others staging-path (ring-reserved, command recorded). Whatever GPU work WAS recorded (staging-path buffer copies and/or any image uploads) still needs a real fence/semaphore; the direct-path portions are already done by the time `flush()` is called. | The single ticket `flush()` returns for a mixed batch covers ONLY the recorded (staging + image) work — since the direct-path portions already completed synchronously before `flush()` was even called, they need no representation in the ticket at all (the ticket's completion signal is correct by construction: it just doesn't need to "wait for" work that already finished). No special mixed-mode logic needed beyond rows 5-8 composing correctly — flagged as a row specifically so this composition is verified by a test, not assumed: a batch mixing both upload kinds produces one ticket whose `wait()`/`isComplete()` behavior matches a staging-only batch of the same recorded commands (the direct-path calls are invisible to ticket semantics, provably, via a readback assertion on BOTH destinations after the ticket completes). |
| 10 | Optional dedicated transfer-queue ACQUISITION via vk-bootstrap | N/A — direct API-surface row; ticket explicitly scopes this to acquisition-only, matching the registry's already-recorded deferral boundary (`docs/superpowers/specs/2026-08-09-toolchain-platform-rhi-design.md:267-274`: "dedicated transfer-queue USE, queue-family ownership transfer... multi-frame in-flight staging" all stay streaming-phase; "D25: pollable UploadTicket from flush(), optional transfer queue ACQUIRED at device creation with graphics fallback... makes this additive"). | consume-now (acquisition + accessor only; NO cross-queue submission this phase) | Verified first-hand in vk-bootstrap (pinned commit `556b79b165386f6c1a18362d30f2a076fdaa2778`, `VkBootstrap.h`): `Device::get_dedicated_queue(QueueType::transfer) const → Result<VkQueue>` (line 762) is the correct, NON-REQUIRING query — it fails gracefully via its `Result` (not a hard physical-device-selection requirement) when no queue family supports transfer-but-not-graphics-nor-compute (line 514: `has_dedicated_transfer_queue()`'s own definition). This is DISTINCT from `PhysicalDeviceSelector::require_dedicated_transfer_queue()` (line 627), which would make device SELECTION fail outright on hardware lacking one — the wrong primitive for an optional/fallback feature. Today `device.cpp:229-333` never calls either; only `get_queue(QueueType::graphics)`/`get_queue(QueueType::present)` exist (lines 271-289), and `device.h:72-74` exposes no transfer-queue accessor at all (verified absent). | `Device::create()` calls `vkbDevice.get_dedicated_queue(vkb::QueueType::transfer)` (and `get_dedicated_queue_index`) AFTER the existing graphics/present acquisition, following the SAME `Result`-checking pattern already used at lines 271-289; on failure, logs a degrade message and leaves the new `Device::hasDedicatedTransferQueue()` accessor returning false (graphics-queue fallback, per D25's own text) — no failure of `Device::create()` itself. Test: on a device WITHOUT a dedicated transfer-family (e.g. lavapipe, which typically exposes a single universal queue family — verify in-task per the same discipline #21 applies to lavapipe BDA), `hasDedicatedTransferQueue()` returns false and `transferQueue()` is either unavailable or aliases the graphics queue, logged once. **Nothing in the Uploader itself submits to this queue in Phase 4** — the accessor existing and being correctly populated (or correctly absent) is the entire acceptance bar. |
| 11 | Sample 04 call-site migration — in-frame-loop blocking flush | N/A — internal migration target. | consume-now | Verified: `samples/04_streaming/main.cpp:907-920` — inside the mid-frame texture-eviction-cycle helper, `uploader.uploadToImage(*newTexture, ...)` (line 915) immediately followed by an unconditional `uploader.flush()` (line 919), which today blocks the calling (main render) thread on the fence wait per upload.cpp:278 — this is precisely the "sample 04 blocks inside the frame loop today" fact `claim-validation-2026-08-18.md` claim 1 cites. Two separate `Uploader::create()` call sites also exist in this same file (lines 1281, 1580, two different sample sub-scenarios) — BOTH need their call sites audited for the same pattern, not just the one flagged in claim-validation. | Line 919's `uploader.flush()` is replaced with `auto ticket = uploader.flush();` and the eviction-cycle's completion-dependent step (the BindlessTable descriptor rewrite for the incoming texture, per the surrounding code's own "DESCRIPTOR REWRITE SAFETY" comment) is deferred until `isComplete(ticket)` polls true on a subsequent frame (or the sample explicitly documents choosing `wait(ticket)` if it decides the eviction-cycle path stays synchronous by design — either is acceptable per plan Task 11's text, but the CURRENT unconditional blocking behavior must become an explicit, reviewable choice, not an accident). Both `Uploader::create()` call sites (1281, 1580) are audited; any other `.flush()` call site in the same file discovered during implementation is migrated identically (this matrix does not claim to have found every call site exhaustively — see Verification health). |
| 12 | `MeshBuffers::create` — explicit blocking convenience via `wait(ticket)` | N/A — internal API-preservation row. | consume-now | Verified: `MeshBuffers::create()` (mesh_buffers.h:40-44, mesh_buffers.cpp) calls `uploader.flush()` itself before returning, documented as "by the time this call returns, both buffers are already fully populated and safe to bind/draw from immediately." This behavior must be PRESERVED byte-identically per both the ticket text and plan Task 11's own constraint ("existing callers that immediately `wait()` are byte-identical in behavior"). | `MeshBuffers::create()`'s internals change from calling `uploader.flush()` (which will no longer block once D25 lands) to `auto ticket = uploader.flush(); uploader.wait(ticket);` — net external behavior (blocks until buffers are populated before returning) is unchanged; a byte-for-byte-behavior regression test (existing `MeshBuffers` tests, unmodified assertions) passes without alteration, proving the convenience wrapper preserves the old contract exactly. |
| 13 | Wall-clock stall test design methodology | Precedent for the METHODOLOGY (timer-around-the-call, not frame-counter proxies) is the ticket's own explicit language, echoing `claim-validation-2026-08-18.md`'s own finding: "Task 15's overlap test criterion ('counters not wall-clock', plan) is exactly the metric that cannot detect a per-frame fence block." No external first-tier-engine precedent independently verified for the specific "assert wall-clock time inside a call stays under N ms" test PATTERN itself (this is a generic real-time-systems testing technique, not something unique to any engine — flagged as such rather than falsely attributing it to a specific external source). | consume-now | N/A — testing-methodology row. | A dedicated test wraps EVERY `flush()`/`isComplete()`/`pumpMain()`-equivalent call in a high-resolution host timer (`std::chrono::steady_clock`, matching this codebase's existing timer usage conventions — verify against what the repo already uses elsewhere for consistency) across N overlapped frames while an upload batch is in flight, and asserts NO individual call's wall-clock duration exceeds an explicit threshold (the ticket's own text: "a timer around the call, not frame counters"). This must run on a build where the underlying fence/semaphore wait is provably NOT hit synchronously (i.e., the test would FAIL against today's pre-D25 code, proving the test is actually discriminating — a regression test that can't fail against the known-bad baseline is not proof of anything). |
| 14 | Validation-layer cleanliness under polled fences | `KhronosGroup/Vulkan-ValidationLayers` issue #5997 (fetched 2026-08-18): a KNOWN false-positive exists — "THREADING ERROR: vkGetFenceStatus(): object of type VkFence is simultaneously used in thread [X] and thread [Y]" — triggered specifically by CONCURRENT calls to `vkWaitForFences`/`vkGetFenceStatus` on the same fence from two DIFFERENT threads; the spec confirms neither function requires host synchronization, so this is a validation-layer bug, not an application error, per the issue thread. | log-don't-drop (awareness only — not expected to trigger under this codebase's own constraints) | N/A — validation-layer-behavior row, not a library-support row. | **Does not apply under D5's own constraint**: `Uploader::flush()`/`isComplete()`/`wait()` are main-thread-only per D5/D25 (unchanged), so this codebase's own design NEVER calls `vkGetFenceStatus`/`vkWaitForFences` on the same fence from two different threads — the known false-positive's trigger condition cannot occur here BY CONSTRUCTION, not by luck. The zero-validation-errors requirement (plan Task 11's own constraint, "zero validation errors with sync validation") should therefore hold without needing a validation-layer version pin or suppression; if it does NOT hold in practice, that is itself a signal the main-thread-only invariant was violated somewhere (a genuine bug, not a known-false-positive to suppress) — this distinction should be stated explicitly in the ticket so a future validation warning here is correctly triaged as a real regression, not dismissed as "the known #5997 issue." Test: the existing sync-validation-enabled CI gate, exercised against the new polling-based overlap test (row 13), passes with zero validation messages. |
| 15 | Cross-queue submission / queue-family ownership transfer — explicitly OUT of scope | N/A — boundary row, confirming what this ticket must NOT do. | **N/A-Phase-4** (deferral is retrofit-safe by the registry's own recorded reasoning) | Verified: the registry's "Upload/transfer asynchrony policy" entry (`docs/superpowers/specs/2026-08-09-toolchain-platform-rhi-design.md:267-274`) explicitly states D25's acquisition-only scope "makes this additive" for the streaming phase — i.e., acquiring the queue handle now does not force any later redesign, because nothing about queue ACQUISITION constrains how ownership-transfer barriers or cross-queue submission get added later (unlike BDA on #21, which genuinely is retrofit-expensive). This is retrofit-SAFE deferral, not retrofit-expensive — correctly scoped as `N/A-Phase-4` per the brief's own test ("does deferring force expensive changes at existing call sites later? If yes it is NOT N/A" — here, no). | No test needed for this row (nothing is implemented); the acceptance criterion IS the boundary itself: code review confirms no Phase-4 call site submits work to `transferQueue()` — its only Phase-4 consumer is logging/diagnostics (row 10). |

---

## Conflicts

None found that contradict the plan/spec/issue text. Row 1 (the
single-fence-reuse hazard) is not a contradiction of the ticket's text —
it is the underlying mechanical reason the redesign is necessary at all,
made explicit and traced to exact line numbers so the implementer
doesn't rediscover it mid-task. Row 8/9's "trivially complete ticket for
direct-path-only batches" is likewise not a contradiction, but it IS a
case the ticket/plan text does not spell out explicitly (it only says
the ticket carries "fence + ring generation," which implicitly assumes
a fence always exists) — flagged prominently as a required row per the
gate assignment's own instruction ("make the semantics explicit").

## New gaps

None found beyond what the ticket/plan/registry already scope. The
"Upload/transfer asynchrony policy" registry entry already anticipates
and correctly bounds everything this matrix found to be out of scope
(row 15) — no capability surfaced here is missing from the planning
universe.

## Verification health

**Verified first-hand (primary source read directly, in full):**
- `upload.h`/`upload.cpp`/`mesh_buffers.h` — complete files, already in
  context before this matrix was written; every row citing a line number
  against these files is a direct read, not an inference.
- `docs/threading.md` — read in full for the D5 contract.
- vk-bootstrap's `QueueType`/`get_dedicated_queue`/
  `require_dedicated_transfer_queue`/`has_dedicated_transfer_queue` API
  surface — read directly from the pinned-commit header.
- `device.cpp`'s current queue-acquisition code (lines 229-333) and
  `device.h`'s accessor surface (lines 72-74) — read directly, confirming
  no transfer-queue machinery exists today.
- `vkGetFenceStatus`/`VK_KHR_timeline_semaphore`/
  `vkGetSemaphoreCounterValue` semantics — confirmed via the Khronos
  registry man pages directly (not a secondary summary source).
- The D3D12 fence-based ring-buffer pattern (row 5/6) — fetched
  Microsoft's own documentation page directly, including its sample code,
  not a secondary description.
- The Vulkan-ValidationLayers #5997 false-positive (row 14) — fetched the
  GitHub issue directly.

**Inferred / lower-confidence, flagged explicitly:**
- Sample 04's COMPLETE set of `.flush()`/`Uploader::create()` call sites
  is NOT independently exhaustively re-verified beyond the three sites
  cited (lines 919, 1281, 1580) — the file is 1500+ lines and this pass
  targeted the sites `claim-validation-2026-08-18.md` and a direct grep
  for `flush()`/`Uploader::create` surfaced; row 11 explicitly instructs
  the implementer to audit for any additional site discovered during
  implementation rather than treating this list as provably complete.
- The Vulkan Documentation Project's "Synchronization and Streaming"
  tutorial (row 2) was read via WebFetch's summarization, not as raw
  page source — treated as directionally reliable (it is Khronos's own
  documentation project, not a third-party blog) but a secondary
  rendering rather than a primary-source line citation the way the VMA
  header reads were.
- Whether lavapipe (CI's driver, per D17) exposes a dedicated
  transfer-only queue family is UNVERIFIED here — row 10's acceptance
  criterion explicitly defers this to in-task empirical verification,
  consistent with the same discipline #21 already applies to lavapipe
  BDA support (this matrix does not duplicate that verification effort).
- General real-time/wall-clock stress-testing methodology (row 13) is
  presented as a generic technique with no specific first-tier-engine
  citation claimed — flagged rather than manufacturing a false precedent.

No dead links encountered.
