# SDD ledger — plan: docs/superpowers/plans/2026-08-10-phase2-shader-resource.md
# Phase 1 complete and released (v0.1.0-phase1); prior ledgers: .superpowers/sdd/2026-08-09-toolchain-platform-rhi/ and .superpowers/sdd/2026-08-10-phase1-completion/
Task 1: complete (commits 30eb296..1f15eb8, review Approved; fetch-marker staleness fix genuinely closes the tracked Phase 1 finding; zig-cc SONAME workaround verified narrowly-scoped with DT_NEEDED still fully versioned; full mutex coverage of all Slang front-end calls; runtime-lib placement proven via readelf/ldd + live 7/7 test run; Windows link gate passed incl. bonus Wine execution of the full compiler suite)
Task 1: minor (deferred to close-out wave): rx_shader_link_smoketest not registered via add_test (build-step gate only, cosmetic); findProfile("sm_6_0") result unvalidated (asymmetric with the capability-lookup fallback beside it); rx_core log::init check-then-act not atomic (inherited, harmless).
Task 1: note for Task 8 (CI): implementer flagged CI cache-key overlap for slang-prebuilt now that both platform archives are fetched — Task 8 must key the Slang cache per-preset or include the platform in the key.
Task 2: fix round 1/5 (2 Important + 1 minor addressed — doc misattribution of the descriptor-type API corrected in reflection.h/report, assertion count corrected 83→73, shape-flag prose fixed; re-review: all addressed, zero functional change; commits 88607a7..ee12102)
Task 2: complete (commits 0d3387e..ee12102, review clean after 1 doc-only fix round; [R:A3] research-file reflection walk empirically disproven and replaced with a verified-correct API combination — the shipped-header-wins rule working as designed; exact-value tests independently re-derived by reviewer against Slang/std430 rules)
Task 2: minor (deferred): set-index gap-filling implemented+documented but untested; rx_shader_layout_types INTERFACE exposes whole include/ dir (guarantee real but coarse); noted for close-out wave if touched again.
Task 3: complete (commits 33faa86..241e5cb, review Approved; live run 12 cases/195 assertions zero validation errors; VkPhysicalDevice signature deviation verified sound with correct per-type limit fields; absurd-capacity rejection genuinely exercised on this hardware)
Task 3: FORWARD ITEM promoted into the plan's Task 6 section: BindlessTable set-0 layout vs PipelineLayoutBuilder reflected layout are NOT compatible by construction (4096 sentinel + no variable-count flag vs capacities + flag) — Task 6 must add the external-set-0-layout substitution parameter to PipelineLayoutBuilder with a focused test. Plan updated accordingly.
Task 3: minor (deferred): two hand-maintained feature-name lists in device.cpp with nothing enforcing sync; logDescriptorIndexingFeatureGaps unexercised end-to-end (correct by reading); zero-capacity/exhaustion guards exercised only by review; per-stage aggregate limits not pre-checked (clean generic failure path exists).
Task 5: review Approved with 1 Important (push-constant offset hardcoded 0, latent/inert — fix round dispatched to worktree implementer). Reviewer independently reproduced pName="main" via slangc+spirv-dis, confirmed single-global-session reuse across fresh Compilers, validated the CMake scoping diagnosis, ran the headless gate live.
Task 5: TRACKED for Task 6 dispatch + close-out: (a) rx_shader_deploy_runtime_libs has a directory-scope variable gap — sibling sample dirs must re-glob locally (Task 5's workaround pattern) until the function is fixed to use a cache variable or generator expression; fix properly when next touching src/rx_shader/CMakeLists.txt. (b) compiler.h's session-reuse doc comment is in tension with same-path compileFromFile reloads (needs a documented caveat; close-out wave).
Task 5: fix round 1/5 (1 addressed — reflected push-constant offset plumbed end-to-end, single-file 27-insertion diff, re-review: ADDRESSED, no scope creep; commits deb8855..857fb45)
Task 5: complete pending merge (branch worktree-agent-a381281ec33251156 @ 857fb45, review clean after 1 fix round; merge deferred until Task 4's agent finishes in the main tree)
Task 4: implementation landed (commit 3fcbc3a; 22 cases/625 assertions green; review dispatched). Closed the tracked Buffer::flush/invalidate gap; added FrameSync::frameNumber().
Task 5: MERGED into main (merge commit 3cd8a26; post-merge combined verification 7/7 green incl. sample_02_hotreload_headless against the full Task 4 engine).
Task 4: review NEEDS FIXES — 1 Critical (ReBAR direct path structurally dead: ALLOW_TRANSFER_INSTEAD on a TRANSFER_SRC-only staging ring never triggers VMA's DEVICE_LOCAL preference; proven against vk_mem_alloc.h source + live run on ReBAR-capable RTX 2080 where the direct path never engaged once) + 3 Important (missing SAMPLED_IMAGE_FILTER_LINEAR_BIT check for linear blits; mipLevels==1 path fully untested; frameNumber() no direct unit test). Fix round 1 dispatched: destination-side ALLOW_TRANSFER_INSTEAD pattern for buffers (skip copy when allocation lands DEVICE_LOCAL|HOST_VISIBLE), images stay staging-only (no VK_EXT_host_image_copy in baseline, documented), plus the three test/check gaps. Mechanism-asserting tests required, not hardware-assuming.
Task 4: fix round 1/5 (4/4 addressed — destination-side ALLOW_TRANSFER_INSTEAD verified against VMA's ContainsDeviceAccess precondition, memcpy+flush non-coherent-safe, CHECK-not-MESSAGE engagement assertion passing live on RTX 2080, byte-exactness both branches; linear-filter bit added; single-mip both-branches test; frameNumber monotonicity test. Re-review: all addressed, no new breakage; commits 6f00fb7..12815a5)
Task 4: complete (commits 0e6f0dc..12815a5 incl. fix round, review clean; 26 cases/703 assertions; Buffer::flush/invalidate gap closed for good)
Task 4: minor (deferred): single-mip FORMAT-fallback path still untested (no portable failing format; logic verified by review twice); direct-path engagement now per-call not per-Uploader (Task 6/7 heads-up, noted in their dispatch context).
Task 6: review Approved (30/30 cases 732/732 assertions live, 5/5 probe gate; matrix-layout diagnosis independently confirmed the only correctly-scoped fix — Slang has no per-declaration majority qualifier; depth-twin masks verified at all 4 sites; set-0 ownership contract traced through every destruction path). Fix round dispatched for the 2 Important debt items (engine-level matrix-layout doc on Compiler::create; parameterize transitionImage aspect + delete the sample's twin) + 2 minors (CACHE PATH comment, third rejection test) — closing now so Task 7 doesn't inherit them.
Task 6: fix round 1/5 (2 Important + 2 minor addressed — engine-level matrix-layout doc with cross-refs, transitionImage aspect parameterization with twin fully deleted and 4 sites migrated, CACHE PATH comments, third rejection test; re-review: all addressed, 31/738 corroborated live, no new breakage; commits 9e46726..bb5fe15)
Task 6: complete (commits b9fda92..1d4f8e7 incl. fix round, review clean; the reflection+bindless+upload integration proof runs verified on real hardware)
Task 7: review NEEDS FIXES — 1 Critical (descriptor-slot REWRITE not frame-lag-protected: release-then-immediate-reregister deterministically rewrites the just-freed slot via LIFO HandlePool while frame N-1 may still dynamically sample it — spec violation invisible to validation; destruction-side math verified correct) + 1 Important (bindless.h's Task 3-era RELEASE-SAFETY CONTRACT comment overclaims rewrite safety, missing the not-dynamically-used condition). Fix round 1 dispatched: defer the slot rewrite to the fence-confirmed point (DeletionQueue-style), tighten the engine contract comment, correct the report's "very likely" to deterministic.
Task 7: fix round 1/5 (all 3 addressed — slot rewrite now deferred inside the same DeletionQueue retirement as the destruction, register+bookkeeping atomic at the fence-confirmed point, no remaining hazard window per end-to-end trace; contract comment tightened with the not-dynamically-used condition; report corrected to deterministic. Re-review: all addressed; commits 412c4fd..ba90eee)
Task 7: complete (commits 732e0c8..ba90eee incl. fix round, review clean; eviction-while-in-flight now FULLY proven — both halves fence-gated)
Task 7: minor (deferred to close-out wave): the deferred registration closure logs-and-drops on registerSampledImage failure (fire-and-forget lambda) where the pre-fix code hard-propagated — unexercised in practice (16 capacity vs 8 resident), flagged by re-review as a real error-handling regression worth a propagation path or a documented acceptance.
Task 8: complete (commits e051006..ca8f4be; both CI jobs REAL green on GitHub — run https://github.com/ywadi/renderer_x/actions/runs/31364667891, linux-native 3m15s + windows-cross-zig 4m44s, both build-budget checks 2-3s/60s). Slang prebuilt cache key fixed to per-job (closing Task 1's tracked cache-effectiveness gap); both ctest steps now also emit --output-junit uploaded as its own artifact (closes Phase 1's deferred -V/--output-junit suggestion); rx_shader_tests confirmed running (and passing) under Wine on the real runner, not just locally. Found and fixed a genuine pre-existing redistribution bug while implementing packaging: sample_01_triangle read its SPIR-V from a hardcoded absolute build-tree path, which would break on any redistributed copy — fixed via SDL_GetBasePath() + a build-time deploy step, same pattern 02/03 already used. Also found and fixed a genuine pre-existing RED MAIN unrelated to this task's own files: two rx_rhi_vk upload_test.cpp cases assumed a transfer-only-usage buffer can never measure directPathCapable()==true "no matter the hardware" (buffer.h's own doc comment) — false on GitHub Actions' lavapipe/llvmpipe build specifically (verified via 3 consecutive real failed runs, same two assertions each time), which has no non-host-visible DEVICE_LOCAL memory type to steer away into; fixed both tests to use the already-established createHostVisibleBuffer() hardware-independent pattern a third test case in the same file already modeled, and corrected the falsified doc comment. All four samples packaged per-platform (tools/package_samples.sh) into rendererx-samples-<platform>.zip workflow artifacts laid out exactly as D2 specifies (01_triangle: binary + 2 .spv files, no Slang libs; 02/03/04: Slang runtime libs + LICENSE + on-disk asset where applicable; slang-llvm/gfx never present) — spot-verified by downloading the actual linux zip from the green run and running sample_01_triangle + sample_02_hotreload headless from the unzipped layout, both PASSED. samples/README.md finalized with a downloadable-artifact section, 01_triangle's own Redistribution section, and corrected Windows copy instructions.
Task 8: note for Task 9 (close-out): the buffer.h/upload_test.cpp fix above is a real, verified finding (not present in either ledger's prior deferred lists) — worth a glance during final whole-branch review in case any other test elsewhere in the suite carries the same "no matter the hardware" assumption about VMA memory-type steering (none found in this task's own grep across rx_rhi_vk/tests, but reviewer's own pass may want to re-confirm).
Task 8: complete (commits 9c59024..6719083, review Approved; reviewer independently downloaded+unzipped+ran both platform artifacts, verified D2 lib lists exactly, confirmed the ca8f4be Uploader-test fix left Task 4's mechanism assertion untouched and strengthened the unrelated staging tests; 9/9 gates green in CI with junit self-verification; budgets 2s/3s of 60s)
Task 8: coordinator decision on the flagged zip-size item: KEEP the per-sample self-contained layout for v0.2.0-phase2 (~136-165MB/platform) — unzip-and-run UX outweighs release-asset size; noted for release notes.
Task 9: STARTED — final whole-branch review over Phase 2's full range, deferred-minors triage, then release v0.2.0-phase2.
